# ทำ vcut เป็นแอป — Mac · Windows

เอกสารนี้วิเคราะห์ว่า vcut_engine ที่มีอยู่ตอนนี้ **ห่อเป็นแอปได้แค่ไหน** ต้องแก้
อะไรบ้าง และทางไหนคุ้มที่จะเดิน — แล้ว[ข้อ 10](#10-เช็กลิสต์งานทั้งหมด) บอกว่าทำ
ไปถึงไหนแล้ว

> อ่านคู่กับ [README.md](README.md) (เอนจินทำงานอย่างไร) ·
> [vcut README.md](vcut%20README.md) (คู่มือใช้งาน) ·
> [desktop/README.md](desktop/README.md) (ตัวห่อ) ·
> [RUN-STOP.md](RUN-STOP.md) (สามทางที่รันได้)

> **อัปเดต 25 ส.ค. 2026 (รอบสอง) — ลงมือทำแล้ว**
>
> **ขอบเขตเปลี่ยนเป็น Mac · Windows เท่านั้น** — iOS ถูกตัดออกจากแผน ส่วนที่
> วิเคราะห์ไว้ยังอยู่ที่ [ข้อ 7](#7-ทำไม-ios-ถึงเป็นคนละเรื่อง) กับ
> [ข้อ 5 ทาง D/E](#5-ห้าทางที่เป็นไปได้) ในฐานะบันทึกว่าทำไมถึงไม่ทำ ไม่ใช่แผนงาน
>
> **กลุ่ม A · B · C ในเช็กลิสต์ทำเสร็จและทดสอบแล้ว** — เอนจินเสิร์ฟหน้าเว็บเอง ·
> พอร์ตสุ่ม · เปลี่ยนโปรเจกต์ได้ · โมเดล whisper โหลดตอนรันครั้งแรก · 8 จุดฝั่ง
> Windows แก้ครบ · เปลือก Electron รันได้ทั้งแบบ dev และแบบ package แล้ว
> เหลือแค่ code signing กับไอคอน (ต้องมี certificate ก่อน)

**สรุปหนึ่งบรรทัด:** Mac กับ Windows ห่อได้จาก codebase เดียว **ด้วย Electron**
(ไม่ใช่ Tauri — ทดสอบแล้วว่า WebKit เล่นสตรีมพรีวิวของเอนจินไม่ได้ ดูข้อ 4) ·
หน้าเว็บ Next.js ไม่ได้ทำให้ยากขึ้นเพราะมันเป็น build step ไม่ใช่ runtime ·
งานทั้งหมดที่แผนบอกว่าต้องทำ **ทำเสร็จแล้ว** เหลือขั้นตอนที่ต้องรอ certificate

---

## สารบัญ

1. [ตอนนี้มันเป็นอะไรอยู่](#1-ตอนนี้มันเป็นอะไรอยู่)
2. [ตารางความยากรายแพลตฟอร์ม](#2-ตารางความยากรายแพลตฟอร์ม)
3. [ชั้นหน้าเว็บ Next.js ที่เพิ่มเข้ามา](#3-ชั้นหน้าเว็บ-nextjs-ที่เพิ่มเข้ามา)
4. [ผลทดสอบจริง และการเลือกตัวห่อ](#4-ผลทดสอบจริง-และการเลือกตัวห่อ)
5. [ห้าทางที่เป็นไปได้](#5-ห้าทางที่เป็นไปได้)
6. [สิ่งที่ต้องแก้จริงเพื่อไป Windows](#6-สิ่งที่ต้องแก้จริงเพื่อไป-windows)
7. [ทำไม iOS ถึงเป็นคนละเรื่อง](#7-ทำไม-ios-ถึงเป็นคนละเรื่อง)
8. [แผนที่เสนอ](#8-แผนที่เสนอ)
9. [ข้อสรุปที่ต้องพูดให้ตรง](#9-ข้อสรุปที่ต้องพูดให้ตรง)
10. [เช็กลิสต์งานทั้งหมด](#10-เช็กลิสต์งานทั้งหมด) ← **สถานะจริง อยู่ตรงนี้**

---

## 1. ตอนนี้มันเป็นอะไรอยู่

vcut **เป็น local web app อยู่แล้ว** ซึ่งบังเอิญเป็นรูปแบบที่ห่อเป็นแอปเดสก์ท็อป
ได้ง่ายที่สุด:

```
[vcut-ui/          Next.js 16 + React 19 · 7,925 บรรทัด · client ล้วน]  ← หน้าหลักวันนี้
[viewer/index.html  479 KB · 7,523 บรรทัด · ไฟล์เดียว]                  ← ของเดิม ยังเสิร์ฟอยู่
                    ↑ HTTP   /engine/* → /api/*
        [serve.py  ThreadingHTTPServer · stdlib ล้วน · 1,475 บรรทัด]
                    ↑ subprocess   [sys.executable, ./vcut, <คำสั่ง>]
        [vcut_engine/  27 โมดูล · 11,701 บรรทัด · stdlib ล้วน]
                    ↑ subprocess
      ffmpeg · ffprobe · whisper-cli · claude (CLI) · yt-dlp
                    ↑
              .vcut/*.json   ← สถานะทั้งหมดของระบบ
```

### สามอย่างที่ทำให้ห่อง่าย

| | ทำไมถึงสำคัญ |
|---|---|
| **หน้าเว็บไม่มีฝั่งเซิร์ฟเวอร์เลย** | ทั้งสองชุดกลายเป็นไฟล์ static ได้ — `viewer/index.html` เป็นอยู่แล้ว ส่วน vcut-ui เป็น client ล้วน ไม่มี API route ไม่มี server action (ดู [ข้อ 3](#3-ชั้นหน้าเว็บ-nextjs-ที่เพิ่มเข้ามา)) |
| **สถานะทั้งหมดเป็นไฟล์ JSON** | ไม่มี DB ไม่มี migration ไม่มี state ในหน่วยความจำที่ต้องย้าย |
| **เอนจินเป็น stdlib ล้วน** | ไม่มี dependency tree ที่จะพังตอน bundle — แค่ Python 3.11 กับ ffmpeg |

### หนึ่งอย่างที่ตัดสินทุกอย่าง

ทั้งระบบตั้งอยู่บนสมมติฐานเดียว: **spawn โปรเซสลูกได้**

บน macOS/Windows สมมติฐานนี้จริง — บน iOS มัน **ผิด** และผิดในระดับที่แก้ไม่ได้
ด้วยการเขียนโค้ดเพิ่ม เพราะเป็นข้อจำกัดของ OS ไม่ใช่ของโค้ด

---

## 2. ตารางความยากรายแพลตฟอร์ม

| ความสามารถที่เอนจินต้องใช้ | macOS | Windows | iOS / iPadOS |
|---|---|---|---|
| Python 3.11 runtime | ✅ ฝังได้ (python-build-standalone) | ✅ ฝังได้ | ⚠️ ฝังได้ แต่ห้ามโหลดโค้ดใหม่จากเน็ต |
| Node.js + `next build` | ✅ **ตอน build เท่านั้น** | ✅ **ตอน build เท่านั้น** | ✅ static ที่ได้ไม่ต้องมี Node ตอนรัน |
| สตรีมพรีวิวสด `/live/` (chunked fMP4) | ⚠️ Chromium เท่านั้น — WKWebView เล่นไม่ได้ ([ข้อ 4](#4-ผลทดสอบจริง-และการเลือกตัวห่อ)) | ✅ WebView2 = Chromium | ❌ WebKit ล้วนทั้งระบบ — **ต้องเป็น HLS ก่อน** |
| `subprocess` เรียกไบนารีภายนอก | ✅ | ✅ | ❌ **ระบบบล็อก fork/exec** |
| ffmpeg / ffprobe เป็น CLI | ✅ | ✅ | ❌ ต้องลิงก์เป็น library |
| ฟิลเตอร์ `ass` (libass + harfbuzz) | ✅ `ffmpeg-full` | ✅ build ของ gyan.dev | ⚠️ ต้องคอมไพล์เอง |
| whisper.cpp | ✅ | ✅ | ✅ (เป็น lib + Metal ได้) |
| `h264_videotoolbox` | ✅ | ❌ ต้องเปลี่ยน encoder | ✅ ผ่าน API |
| ฟอนต์ไทย `Sukhumvit Set` | ✅ มากับเครื่อง | ❌ ไม่มี | ⚠️ มี แต่ลิขสิทธิ์คนละเรื่อง |
| ลิขสิทธิ์ x264 (GPL) | ✅ นอกสโตร์ | ✅ นอกสโตร์ | ❌ **GPL ขัดกับเงื่อนไข App Store** |
| **สรุป** | **ห่อได้เลย** | **แก้ ~8 จุด** | **คนละโปรเจกต์** |

---

## 3. ชั้นหน้าเว็บ Next.js ที่เพิ่มเข้ามา

เอกสารรุ่นแรกเขียนตอนที่หน้าเว็บยังเป็น `viewer/index.html` ไฟล์เดียว — ตอนนี้ไม่ใช่แล้ว
หน้าหลักคือ [vcut-ui/](vcut-ui/) · Next.js 16 + React 19 · 26 ไฟล์ · 7,925 บรรทัด
(ไฟล์เดิมยังอยู่และยังถูกเสิร์ฟที่ `/` แต่ไม่ได้พัฒนาต่อ)

**ข้อสรุปทุกข้อของเอกสารนี้ยังใช้ได้** เพราะสิ่งที่เพิ่มเข้ามาเป็น *build step*
ไม่ใช่ *runtime dependency* — แอปที่ส่งถึงผู้ใช้ไม่ต้องมี Node.js อยู่ในเครื่อง

| ข้อเท็จจริงจากโค้ด | ผลต่อการห่อเป็นแอป |
|---|---|
| ไม่มี API route · ไม่มี server action สักตัว | Next ทำหน้าที่แค่ bundler — `next build` แล้วได้ไฟล์ static ล้วน |
| ทุกไฟล์ที่ถือ state ประกาศ `"use client"` | ไม่มีอะไรต้องรันฝั่งเซิร์ฟเวอร์ตอนใช้งานจริง |
| browser API ที่ใช้จริงมีแค่ `window.addEventListener` กับ `window.innerWidth` — ไม่มี `localStorage` ไม่มี `AudioContext` ไม่มี File System Access | ไม่ต้องกลัวว่า WKWebView (Mac) กับ WebView2 (Windows) รองรับไม่เท่ากัน |
| `public/` 24 MB (bgm 17 · stickers 5.7 · sfx 1.3) | สังเคราะห์เองด้วย numpy ตอน build ([gen_bgm.py](vcut-ui/scripts/gen_bgm.py) · [gen_sfx.py](vcut-ui/scripts/gen_sfx.py)) — ไม่มีลิขสิทธิ์ และ **numpy ไม่ใช่ dependency ตอนรัน** |
| จุดต่อกับเอนจินอยู่บรรทัดเดียว | [lib/api.ts:3](vcut-ui/lib/api.ts#L3) — `export const engine = "/engine"` |

### สี่จุดที่ต้องแก้เพราะ Next.js

> ✅ **แก้ครบทั้งสี่จุดแล้ว** — ตารางข้างล่างเก็บไว้เพราะมันอธิบาย *ทำไม* ถึงต้อง
> แก้แบบนั้น ส่วนแก้ไปยังไงอยู่ที่ [ข้อ 10 กลุ่ม A](#a--งานร่วม--เสร็จแล้ว)

| # | จุด | ปัญหา | ทางแก้ |
|---|---|---|---|
| **N1** | [vcut-ui/next.config.ts](vcut-ui/next.config.ts) | proxy `/engine/*` ทำด้วย `rewrites()` ซึ่งเป็นความสามารถของ **Next server เท่านั้น** — พอสั่ง `output: "export"` ทุกคำขอ `/engine/*` จะกลายเป็น 404 ทันที | ให้ [serve.py](vcut_engine/serve.py) เสิร์ฟไฟล์ static ที่ export ออกมาเอง (แก้ route `/` ให้ชี้โฟลเดอร์ `out/` แทน `VIEWER`) แล้วรับ `/engine/*` เป็นชื่ออื่นของ `/api/*` — **ทำแบบนี้แล้วไม่ต้องแตะ CORS และไม่ต้องแตะ Host guard เลย** |
| **N2** | [serve.py:797](vcut_engine/serve.py#L797) | ทางตรงข้ามของ N1 คือให้ webview ยิงไป `http://127.0.0.1:<port>` ตรง ๆ — ต้องเพิ่ม CORS ทั้งชุด และ `_guard()` จะตอบ 403 ให้ตัวห่อที่ใช้ custom scheme (เช่น `tauri://localhost`) ทันที · Electron ที่ `loadURL` เป็น `http://127.0.0.1:<port>` ตรง ๆ ไม่เจอปัญหานี้ | เลี่ยงด้วย N1 · ถ้าจำเป็นจริงค่อยเติม origin ที่อนุญาตแบบระบุชัด อย่าเปิดกว้าง |
| **N3** | [serve.py:1461](vcut_engine/serve.py#L1461) | พอร์ต 8765 ตายตัว — เปิดสองหน้าต่างหรือมีของอื่นจองพอร์ตอยู่ก็สตาร์ตไม่ขึ้น | bind พอร์ต `0` แล้วพิมพ์พอร์ตจริงออก stdout ให้ตัวห่ออ่านไปเปิด webview |
| **N4** | [serve.py:1438](vcut_engine/serve.py#L1438) | โปรเจกต์ถูกล็อกตั้งแต่ตอนสตาร์ต (`-c projects/x.toml`) ไม่มี API เปลี่ยน → เมนู "เปิดโปรเจกต์…" ของแอปเดสก์ท็อปทำไม่ได้ | ให้ตัวห่อ restart sidecar ด้วย config ใหม่ (ง่ายสุด) หรือเพิ่ม `POST /api/project` ที่สร้าง `Ctx` ใหม่ |

### ⚠️ ห้าม freeze เอนจินด้วย PyInstaller

[serve.py:1329](vcut_engine/serve.py#L1329) และอีก 3 จุด spawn ตัวเองด้วย
`[sys.executable, str(ctx.launcher), ...]` — ถ้า freeze แล้ว `sys.executable` จะกลายเป็น
ตัวแอปเอง ไม่ใช่ python ทำให้ **ทุกปุ่มที่สั่งงานในหน้าเว็บพังพร้อมกันหมด**
ต้องฝัง Python จริง (python-build-standalone) เท่านั้น

---

## 4. ผลทดสอบจริง และการเลือกตัวห่อ

เอกสารรุ่นก่อนวางตัวเลือกตัวห่อไว้สามตัวโดยยังไม่ตัดสิน — **ตอนนี้ตัดสินแล้วด้วย
การทดสอบ ไม่ใช่การเดา** เปิดหน้าเว็บด้วย Safari (= WKWebView ตัวเดียวกับที่ Tauri
กับ pywebview ใช้บน macOS) แล้วกดเล่นพรีวิว ผลคือ **จอดำ + "The operation is not
supported."** ซึ่งเป็นข้อความของ `MEDIA_ERR_SRC_NOT_SUPPORTED`

### 4.1 หลักฐาน — ยิงเข้าเอนจินที่รันอยู่จริง

**สายวิดีโอไม่ได้เสีย** ดูดสตรีมสดมา 3 MB แล้วให้ ffprobe อ่าน:

```
codec_name=h264   codec_tag_string=avc1     ← WebKit เล่นได้แน่นอน
codec_name=aac    codec_tag_string=mp4a     ← WebKit เล่นได้แน่นอน
```

**หัว HTTP ของ `/live/` คือตัวปัญหา**

```
HTTP/1.1 200 OK
Content-Type: video/mp4
Transfer-Encoding: chunked
      ← ไม่มี Content-Length
      ← ไม่มี Accept-Ranges
```

ส่ง `Range: bytes=0-1` เข้าไป (สิ่งที่ WebKit ถามก่อนเสมอ) → **ถูกเมิน ตอบ 200 เหมือนเดิม
ไม่ใช่ 206** ส่วนเส้นอื่นตอบถูกต้องหมด:

```
/seg/<ชิ้น>   → 206 · Accept-Ranges: bytes · Content-Range: bytes 0-1/3486760   ✅
/clip/<คลิป>  → 206 · Accept-Ranges: bytes · Content-Range: bytes 0-1/5570619   ✅
```

**สรุป:** WebKit บังคับให้ `<video src>` ที่เป็น `video/mp4` ต้องเลื่อนหัวอ่านด้วย
byte-range ได้ ส่วน [page.tsx](vcut-ui/app/page.tsx) โยน URL นั้นเข้า `<video>` ตรง ๆ
โดยไม่มี `MediaSource` เลยสักตัว — Chromium ยอมเล่นแบบ progressive แต่ WebKit ไม่ยอม
คอมเมนต์ที่ [serve.py:879-882](vcut_engine/serve.py#L879-L882) รู้อยู่แล้วครึ่งหนึ่ง
(ว่า `v.seekable` ว่าง) สิ่งที่ยังไม่รู้คือ WebKit ไม่ได้แค่ห้าม seek — **มันไม่ยอม
เริ่มเล่นตั้งแต่แรก**

| เส้นทาง | Chromium | WebKit |
|---|---|---|
| `/live/<token>` สตรีมสด | ✅ | ❌ **พังจุดเดียวนี้** |
| `/seg/` · `/clip/` · `/preview/` · `/out` | ✅ | ✅ |

### 4.2 ตารางเทียบตัวห่อ — ตัดสินจากโค้ดนี้ ไม่ใช่จากทั่วไป

| เครื่องมือ | เล่น `/live/` ได้ไหม | ภาษาที่ต้องเพิ่ม | ขนาดรวม | ผล |
|---|---|---|---|---|
| **Electron** ★ | ✅ Chromium ตัวเดียวกันทั้งสอง OS เขียนครั้งเดียว | ไม่มี (TS/Node มีอยู่แล้ว) | ~300 MB | **เลือกตัวนี้** |
| Tauri | ❌ Mac ใช้ WKWebView = ตัวที่เพิ่งพิสูจน์ว่าเล่นไม่ได้ | Rust (เครื่องยังไม่มี `cargo`) | ~160 MB | ตก |
| pywebview | ❌ Mac ใช้ WKWebView เหมือนกัน | ไม่มี | ~155 MB | ตกด้วยเหตุผลเดียวกัน |
| Wails | ❌ webview เดียวกับ Tauri | Go | ~165 MB | ไม่มีข้อได้เปรียบเหนือ Tauri |
| ไม่ห่อ เปิดเบราว์เซอร์ให้ | ⚠️ ผู้ใช้ที่ default เป็น Safari เจอปัญหาเดียวกัน | ไม่มี | ~150 MB | ใช้ตอน dev ได้ ไม่ใช่ "แอป" |

### 4.3 ทำไมเหตุผลเรื่องขนาดถึงไม่ชนะในโปรเจกต์นี้

เหตุผลหลักที่คนเลือก Tauri คือเล็กกว่า Electron ~140 MB — แต่ตัวเลขจริงของแอปนี้คือ:

| ของที่ต้องมี | ขนาดจริง |
|---|---|
| whisper model ที่ [default.toml:50](config/default.toml#L50) ชี้อยู่ (`ggml-large-v3-turbo.bin`) | **1.5 GB** |
| ffmpeg ที่มี libass | ~80 MB |
| python-build-standalone | ~40 MB |
| `next build` static + `public/` | ~25 MB |

> ⚠️ **แก้ตัวเลขในเอกสารรุ่นก่อน** ที่เขียนว่า whisper "+ model (small) ~150 MB" —
> ของจริงที่ config ใช้อยู่คือ large-v3-turbo **1.5 GB** ซึ่ง**ห้ามใส่ installer**
> ต้องโหลดตอนรันครั้งแรกลง `~/.cache/whisper/` (ที่เดิมที่โค้ดหาอยู่แล้ว)

140 MB ในแอปที่ผู้ใช้ต้องโหลด model 1.5 GB ตอนเปิดครั้งแรก และทำงานกับฟุตเทจระดับ
GB ทุกวัน **ไม่ใช่ราคาที่ควรจ่ายด้วยการเอาฟีเจอร์หลักไปเสี่ยง**

### 4.4 สิ่งที่ Electron ปลดล็อกเพิ่ม

| ของที่ได้ | ต่อกับโค้ดปัจจุบันตรงไหน |
|---|---|
| ลากคลิปจาก Finder/Explorer ลงแอปตรง ๆ | ตอนนี้ [api.ts:475](vcut-ui/lib/api.ts#L475) ยัดไฟล์ทั้งก้อนผ่าน `xhr.send(file)` — ฟุตเทจ 3 GB วิ่งผ่าน renderer ทั้งดุ้น · เปลี่ยนเป็นส่ง path ด้วย `webUtils.getPathForFile()` แล้วให้เอนจินก๊อปเอง |
| เมนูบาร์จริง | คีย์ลัดมีอยู่แล้วที่ [page.tsx](vcut-ui/app/page.tsx) (`Cmd+S` ฯลฯ) แค่ผูกเข้าเมนู |
| `.dmg` notarized + `.exe` NSIS + auto-update | `electron-builder` ตัวเดียวจบทั้งสอง OS |

### 4.5 สิ่งที่ทำไปแล้วเพื่อรับมือเรื่องนี้

แม้จะเลือก Electron (ซึ่งทำให้ปัญหานี้ไม่โผล่) หน้าเว็บก็ยังต้องรับมือได้เอง เพราะ
**ยังเปิดด้วยเบราว์เซอร์ตรง ๆ ตอน dev และเพราะ iPad ในเฟส 2 คือ WebKit ล้วน**:

| ทำแล้ว | อยู่ที่ไหน |
|---|---|
| จับ `error` ของ `<video>` แล้วขึ้นข้อความไทยตามรหัส `MediaError.code` | [page.tsx](vcut-ui/app/page.tsx) — เดิมไม่มี `onError` เลยสักตัว ผู้ใช้จึงเห็นข้อความดิบของ WebKit |
| โหมดสำรอง "เล่นทีละชิ้น" — เล่น `/seg/` ต่อกันเอง อุ่นชิ้นถัดไปไว้ล่วงหน้า | ใช้ `/seg/` ที่รองรับ Range อยู่แล้ว จึง **เลื่อนหัวอ่านในชิ้นได้จริง** ต่างจากสตรีมสด |
| สลับโหมดอัตโนมัติ 2 ชั้น — เดา WebKit จาก UA ไว้ก่อน + ถอยเองเมื่อเจอ error รหัส 4 | ชั้นหลังทำงานกับทุกเบราว์เซอร์ที่เล่นไม่ได้ ไม่ว่าจะเดาถูกหรือไม่ |

**ไม่ได้แตะโค้ด Python เลยสักบรรทัด**

### 4.6 ที่ยังเหลือ — HLS (จำเป็นก่อนขึ้นเฟส 2)

โหมดทีละชิ้นแก้อาการได้ แต่ไม่ได้แก้ต้นเหตุ ทางที่ถูกต้องระยะยาวคือเปลี่ยน `/live/`
เป็น HLS:

```python
# แทน  -movflags frag_keyframe+empty_moov -f mp4 pipe:1
-f hls -hls_time 2 -hls_playlist_type event
-hls_segment_type fmp4 -hls_flags delete_segments+append_list
```

แล้วเสิร์ฟ `.m3u8` กับ `.m4s` ผ่าน `_range_file` เดิมที่ทำงานถูกต้องอยู่แล้ว

- Safari · iOS · iPadOS เล่น `<video src="…m3u8">` ได้ native ไม่ต้องมี library
- Chromium ต้องเพิ่ม `hls.js` (~400 KB)
- **ของแถมที่คุ้มที่สุด: ได้ seek จริง** เลิกต้องขอสตรีมใหม่ทุกครั้งที่ย้ายหัวเล่น
- ราคา: เลิก "ไม่แตะดิสก์" ต้องเขียน segment ชั่วคราวแล้วเก็บกวาด (มี `sweep_dir`
  ใน [util.py](vcut_engine/util.py) อยู่แล้ว)

---

## 5. ห้าทางที่เป็นไปได้

### ทาง A — ห่อของเดิมด้วย Electron → Mac + Windows ★

**แรง: 2–3 สัปดาห์ · ความเสี่ยง: ต่ำ · ได้ 2 ใน 3 แพลตฟอร์ม**

ตัวห่อทำแค่ 3 อย่าง:

1. สตาร์ต `serve.py` เป็น sidecar (พอร์ตสุ่ม ไม่ใช่ 8765 ตายตัว — [N3](#3-ชั้นหน้าเว็บ-nextjs-ที่เพิ่มเข้ามา))
2. เปิด webview ชี้ `127.0.0.1:<port>` ซึ่งเสิร์ฟทั้งไฟล์ static ของ vcut-ui และ `/api/*`
   จาก origin เดียวกัน ([N1](#3-ชั้นหน้าเว็บ-nextjs-ที่เพิ่มเข้ามา))
3. ปิดโปรเซสลูกให้สะอาดตอนออก — รวมถึง ffmpeg ที่ `serve.Job` spawn ไว้

โครงร่างที่ต้อง bundle:

```
vcut.app/  (หรือ vcut/ บน Windows)
  python/          python-build-standalone        ~40 MB
  bin/ffmpeg       build ที่คอมไพล์ libass มาด้วย  ~80 MB
  bin/ffprobe
  bin/whisper-cli  (model 1.5 GB โหลดตอนรันครั้งแรก)  ~5 MB
  fonts/           Noto Sans Thai + fonts.conf     ~1 MB
  ui/              ผลของ `next build` (static)     ~25 MB
  vcut_engine/  viewer/  config/
```

installer ~150 MB + Electron ~150 MB = **~300 MB** · แล้วโหลด whisper model 1.5 GB
ตอนเปิดครั้งแรก

**เลือกตัวห่อ: Electron** — ตัดสินด้วยผลทดสอบ ไม่ใช่ความชอบ เหตุผลเต็มและตาราง
เทียบทั้ง 5 ตัวอยู่ที่ [ข้อ 4](#4-ผลทดสอบจริง-และการเลือกตัวห่อ) สรุปสั้น: ทุกตัวที่
ไม่ใช่ Electron ใช้ WKWebView บน macOS ซึ่งเล่นสตรีมพรีวิวของเอนจินไม่ได้

ตัวห่อไม่ต้องแบก Node.js ไปเป็น runtime เพราะ `next build` เกิดขึ้นบนเครื่องที่ปั้นแอป
ไม่ใช่บนเครื่องผู้ใช้ (Electron มี Node ในตัวสำหรับ main process อยู่แล้ว)

**ผลลัพธ์: Mac + Windows จาก codebase เดียว และหน้าเว็บที่ทำมาทั้งหมดใช้ต่อได้ 100%**

---

### ทาง B — เปลือกเนทีฟ + เอนจิน Python เป็น sidecar

**แรง: 2–3 เดือน/แพลตฟอร์ม · ความเสี่ยง: ปานกลาง**

SwiftUI สำหรับ Mac · WinUI 3 สำหรับ Windows คุยกับ `serve.py` ผ่าน HTTP เดิม
ได้ drag-drop จริง · Finder/Explorer integration · เมนูบาร์ · ไทม์ไลน์ที่ลื่นกว่า HTML

**คุ้มก็ต่อเมื่อ** ไทม์ไลน์กับพรีวิวของขั้น 4/5 เริ่มอืดจน HTML รับไม่ไหว —
ตอนนี้ยังไม่มีสัญญาณนั้น ตรงกันข้าม vcut-ui ทำไทม์ไลน์ 6 เลเยอร์
([Timeline.tsx](vcut-ui/components/Timeline.tsx) 653 บรรทัด) กับพรีวิวที่ลากจัดตำแหน่งได้
([Preview.tsx](vcut-ui/components/Preview.tsx) 791 บรรทัด) ไปแล้วบน HTML ล้วน —
เดินทางนี้แปลว่าต้องเขียนสองอย่างนั้นใหม่ทั้งหมด แล้วดูแล UI สองชุดตลอดไป

---

### ทาง C — เขียนเอนจินใหม่เป็น Go / Rust

**แรง: 3–4 เดือน · ประโยชน์: น้อย — ไม่แนะนำ**

จะได้ไฟล์เดียวไม่ต้องแบก Python runtime แต่ 11,701 บรรทัดที่มีอยู่ทำอยู่แค่
*ประกอบสตริง + regex แกะตัวเลข + เขียน JSON* ซึ่งเป็นสิ่งที่ Python ทำได้ดีอยู่แล้ว
งานหนักทั้งหมดอยู่ใน ffmpeg ที่เป็น C อยู่แล้ว — เขียนใหม่ไม่ได้เร็วขึ้นเลยสักนิด

เหตุผลเดียวที่จะทำคือถ้าจะไป iOS จริง ๆ (ดูทาง E)

---

### ทาง D — เอนจินเป็นเซิร์ฟเวอร์ + iOS เป็น thin client ★

**แรง: 1–2 เดือนสำหรับ client · นี่คือทางเดียวที่ iOS เกิดได้ในเวลาที่รับได้**

```
[Mac / mini PC / VPS]  เอนจินเต็ม ทำงานหนักทั้งหมด
        │  HTTP + WebSocket
        ├── iPhone / iPad   ดูคลัง · ติ๊กเอา-ไม่เอา · ลากไทม์ไลน์ · กด render · ดูผล
        └── Mac / Windows   vcut-ui ตัวเดิม ไม่ต้องแก้อะไร
```

เหมาะกับความจริงของงานนี้มาก — ฟุตเทจ 3.1 GB · `render` ครั้งแรก ~40 นาที ไม่ใช่
งานที่ควรอยู่บนมือถืออยู่แล้ว มือถือทำหน้าที่ที่มันเก่ง: **ตัดสินใจกับดูผล**
ส่วนเครื่องใหญ่ลงมือ

โดยเฉพาะ iPad จะเป็นอุปกรณ์ที่ดีมากสำหรับสองขั้นที่นิ้วทำได้ดีกว่าเมาส์:

```
ขั้น 1  ติ๊กเลือกคลิปทีละตัว · หมุนคลิป · ลากสลับลำดับ
ขั้น 3  ลากไทม์ไลน์จัดลำดับ · ติ๊กรับข้อเสนอของ AI
```

⚠️ **ต้องทำ HLS ก่อน ไม่งั้น iPad เจอจอดำทันที** — iOS/iPadOS บังคับให้ทุกเบราว์เซอร์
ใช้ WebKit ซึ่ง[พิสูจน์แล้วว่าเล่น `/live/` ไม่ได้](#4-ผลทดสอบจริง-และการเลือกตัวห่อ)
ไม่ว่าจะเปิดด้วย Safari, Chrome เวอร์ชัน iOS หรือแอป SwiftUI ที่ฝัง WKWebView ก็ตาม
— นี่ไม่ใช่ข้อจำกัดของ client ที่จะเขียน แต่เป็นของสิ่งที่เอนจินส่งออกมา จึงต้องแก้
ที่ [ข้อ 4.6](#46-ที่ยังเหลือ--hls-จำเป็นก่อนขึ้นเฟส-2) ก่อนเริ่มเขียน client
(โหมด "เล่นทีละชิ้น" ที่ทำไว้แล้วพอให้ใช้งานได้ แต่ 208 ชิ้น = 208 รอยต่อ ไม่เหมาะ
เป็นของถาวรบนอุปกรณ์ที่เน็ตช้ากว่า)

⚠️ **มีงานความปลอดภัยจริงที่ต้องเพิ่มก่อน** — ตอนนี้ `serve.py` ผูก `127.0.0.1`
อย่างเดียวและตรวจ `Host` ทุก request **โดยเจตนา** ไม่มี login เพราะไม่ต้องมี
การเปิดออกนอก loopback แปลว่าต้องสร้างใหม่ทั้งชั้น:

| ต้องเพิ่ม | เพราะ |
|---|---|
| token / auth จริง | ทุกปุ่มในหน้าเว็บ spawn โปรเซสได้ = RCE ถ้าไม่มีชั้นกั้น |
| TLS | สตรีมวิดีโอกับ transcript วิ่งผ่านเน็ตแล้ว |
| rate limit + งานพร้อมกันได้ทีละหนึ่ง | เอกสารเตือนไว้แล้วว่าอย่าสั่งงานพร้อมกันสองหน้าต่าง มันเขียน `.vcut/` ชุดเดียวกัน |
| จำกัด path ที่เสิร์ฟได้ | ตอนนี้ `SAFE_NAME` กันได้ระดับหนึ่ง แต่ออกแบบมาสำหรับ loopback |

---

### ทาง E — iOS เนทีฟเต็มรูป

**แรง: 6–12 เดือน · ความเสี่ยง: สูงมาก — ไม่แนะนำ**

ต้องทิ้งแทบทุกอย่างที่ [README.md](README.md) อธิบายว่าเป็นหัวใจของการออกแบบ:

| ตอนนี้ | บน iOS ต้องเป็น |
|---|---|
| `subprocess` เรียก ffmpeg | ลิงก์ libavcodec/libavfilter เป็น xcframework เอง (FFmpegKit เลิก maintain แล้ว) |
| `build_vfilter()` ประกอบสตริงส่ง ffmpeg | AVMutableVideoComposition + Core Image หรือเรียก libavfilter ผ่าน C API |
| ฟิลเตอร์ `ass` ของ libass | คอมไพล์ libass + harfbuzz + fribidi เข้า app — ทำได้ แต่หนัก |
| `-f concat -c copy` | AVAssetExportSession — ไม่มี stream-copy concat ที่เทียบเท่าตรง ๆ |
| ดัก stderr ด้วย regex (motion · LUFS · silence) | ต้องหาทางวัดใหม่ทั้งหมด — `ebur128` กับ `silencedetect` เป็น filter ของ ffmpeg |
| `claude` CLI | เรียก Anthropic API ตรง |
| `ThreadPoolExecutor` นั่งรอ subprocess | GCD + ต้องรับมือ thermal throttling ของมือถือ |
| ไฟล์ 3.1 GB ใน `.vcut/segments/` | sandbox + พื้นที่เครื่อง + iOS ฆ่าแอปที่กินแรมเยอะตอนอยู่เบื้องหลัง |

**และตัวที่ฆ่าโปรเจกต์คือลิขสิทธิ์:** x264 เป็น GPL ซึ่งเข้ากันไม่ได้กับเงื่อนไข
App Store ต้อง build ffmpeg แบบ LGPL ที่ไม่มี x264 แล้วเข้ารหัสด้วย VideoToolbox แทน
— ทำได้ แต่เป็นงานที่ต้องรู้จริงเรื่อง build system ของ ffmpeg

---

## 6. สิ่งที่ต้องแก้จริงเพื่อไป Windows

> ✅ **แก้ครบทั้ง 8 จุดแล้ว** และรวมเรื่อง OS ทั้งหมดไว้ที่โมดูลเดียว
> [vcut_engine/host.py](vcut_engine/host.py) — ดูว่าแก้เป็นอะไรที่
> [ข้อ 10 กลุ่ม C](#c--ฝั่ง-windows--แก้ครบ-8-จุด) · ตารางข้างล่างเก็บไว้เพราะมัน
> อธิบายว่า *อาการ* ของแต่ละจุดคืออะไร ซึ่งยังต้องใช้ตอนไปทดสอบบนเครื่องจริง
>
> ⚠ ที่ตอนสำรวจนับผิดไปสองจุด: `Sukhumvit Set` ฮาร์ดโค้ดอยู่ **6 จุด** ไม่ใช่ 4
> (`fx.py` มีสองที่ · `journey.py` มีสองที่) และ `text=True` ที่ไม่ระบุ encoding
> มี **4 จุด** ไม่ใช่ 2 (`caption.py` กับ `provider.py` ตกสำรวจไป)

ไล่จากโค้ดจริง ไม่ใช่การเดา

| # | จุด | ปัญหา | ทางแก้ |
|---|---|---|---|
| 1 | [render.py:189](vcut_engine/render.py#L189) · [settings.py:353](vcut_engine/settings.py#L353) · [config/default.toml:212](config/default.toml#L212) | `h264_videotoolbox` เป็น macOS อย่างเดียว | เลือก default ตาม OS: `h264_nvenc` / `h264_qsv` / `h264_amf` / fallback `libx264` |
| 2 | [caption.py:102-103](vcut_engine/caption.py#L102) | ฮาร์ดโค้ด `/opt/homebrew/...` ตอนหา ffmpeg ที่มีฟิลเตอร์ `ass` | ทำ resolver แยกตาม OS — Windows ใช้ build ของ gyan.dev ที่มี libass มาแล้ว |
| 3 | [caption.py:34](vcut_engine/caption.py#L34) · [fx.py:144](vcut_engine/fx.py#L144) · [fxtext.py:411](vcut_engine/fxtext.py#L411) · [journey.py:48](vcut_engine/journey.py#L48) | ฟอนต์ตั้งต้น `Sukhumvit Set` ไม่มีบน Windows → libass fallback แล้วสระ/วรรณยุกต์เพี้ยน (มี 4 จุดที่ฮาร์ดโค้ดชื่อนี้ ต้องแก้ให้ครบ) | bundle **Noto Sans Thai** ไปกับแอป + ตั้ง `FONTCONFIG_FILE` ให้ libass เห็น |
| 4 | [caption.py:535](vcut_engine/caption.py#L535) | `-vf ass='{fpath}'` — path Windows มี `\` และ `C:` ซึ่งตัวแยกฟิลเตอร์ของ ffmpeg กินเอง | escape เป็น `C\:/path/x.ass` หรือ `cwd` เข้าโฟลเดอร์แล้วใช้ path สัมพัทธ์ |
| 5 | [util.py:70](vcut_engine/util.py#L70) · [serve.py:189](vcut_engine/serve.py#L189) | `text=True` **ไม่ระบุ encoding ทั้งสองจุด** → Windows ใช้ locale (cp874) → ชื่อไฟล์/log ภาษาไทยเพี้ยนทั้งระบบ รวมถึง log ที่ไหลขึ้นหน้าเว็บ | ใส่ `encoding="utf-8", errors="replace"` ทุกจุดที่เรียก subprocess |
| 6 | [listen.py:86](vcut_engine/listen.py#L86) · [provider.py:134](vcut_engine/provider.py#L134) · [music.py:270](vcut_engine/music.py#L270) | สมมติว่า `whisper-cli` / `claude` / `yt-dlp` อยู่ใน PATH — สองตัวแรกมีช่อง config ให้ชี้ path เต็มอยู่แล้ว (`listen.binary` · `ai.binary`) แต่ `yt-dlp` เรียกผ่าน `shutil.which` ตรง ๆ ไม่มีช่องให้ตั้ง | ตั้ง default ของ `*.binary` ให้ชี้ `bin/` ในแอป + เพิ่มช่อง config ให้ yt-dlp |
| 7 | [serve.py:174](vcut_engine/serve.py#L174) | `proc.terminate()` บน Windows ไม่ลามถึงลูกของลูก → ffmpeg กลายเป็นลูกกำพร้ากินซีพียูต่อ | ใช้ Job Object บน Windows หรือ `taskkill /T /F /PID` |
| 8 | [vcut README.md](vcut%20README.md) | เอกสารสอน `pkill` · `lsof` · `pgrep` | ตัวห่อจัดการวงจรชีวิตโปรเซสให้เอง — ปัญหาหายไปเอง เหลือแค่แก้เอกสาร |

### สิ่งที่ไม่ต้องแก้เลย

```
pathlib ทุกจุด              ·  content-hash cache (seg_key)
โครงสร้าง JSON ทั้งหมด       ·  ระบบ preset / config merge
viewer/index.html           ·  ตรรกะ prepare / compose / render / fx
vcut-ui ทั้ง 7,925 บรรทัด    ·  ThreadPoolExecutor
schema ของ settings.py      ·  Progress bar parsing
```

**คิดเป็นเกินกว่า 90% ของโค้ด** — เพราะหลักการ "Python เป็นสมอง ffmpeg เป็นกล้ามเนื้อ"
ทำให้ส่วนที่ผูกกับ OS มีน้อยมากตั้งแต่ต้น

---

## 7. ทำไม iOS ถึงเป็นคนละเรื่อง

ไม่ใช่เพราะโค้ดเขียนไม่ดี แต่เพราะ **iOS ห้ามสิ่งที่เป็นแกนของการออกแบบนี้พอดี**

```
หลักการของ vcut                          ข้อห้ามของ iOS
───────────────────────────────────      ─────────────────────────────
"ไม่มี library ภายนอกเลย                ห้าม fork/exec ไบนารีอื่น
 เรียก CLI ผ่าน subprocess แทน"          → CLI ทั้ง 4 ตัวใช้ไม่ได้ทันที

"ให้ ffmpeg ลงมือทำแทน"                  ffmpeg ต้องเป็น .xcframework
                                          → เลิกเป็นสตริง กลายเป็น C API

"ffmpeg ตัวไหนก็ได้ที่มี libass"          ต้องคอมไพล์เองทั้ง toolchain
                                          → และเลี่ยง GPL ให้ได้ด้วย
```

จุดที่ต้องเข้าใจ: **สิ่งที่ทำให้เอนจินนี้แข็งแรงบนเดสก์ท็อป คือสิ่งเดียวกันที่
ทำให้มันลง iOS ไม่ได้** ไม่มีทางได้ทั้งสองอย่าง

ทางที่รักษาสิ่งที่สร้างมาไว้ได้ครบคือ **ให้ iOS เป็นหน้าจอ ไม่ใช่เครื่องจักร** (ทาง D)

---

## 8. แผนที่เสนอ

### เฟส 1 — Mac + Windows · ✅ ทำแล้ว

```
1. ✅ N1–N4 — serve.py เสิร์ฟ static เอง · พอร์ตสุ่ม · POST /api/project
2. ✅ 8 จุดฝั่ง Windows — รวมไว้ที่ vcut_engine/host.py โมดูลเดียว
3. ✅ ห่อด้วย Electron — desktop/ · sidecar + BrowserWindow + ฆ่าต้นไม้โปรเซส
4. ◐ bundle — ui/ กับ fonts/ พร้อมแล้ว · bin/ กับ python/ ต้องวางเอง
5. ☐ code signing + notarization — รอ certificate
```

**ที่ทดสอบไปแล้วจริง (ไม่ใช่แค่เขียนโค้ด):**

| ทดสอบ | ผล |
|---|---|
| หน้า vcut-ui โหลดครบทุก asset จาก `serve.py` โดยไม่มี `next dev` | 19/19 asset = 200 · console ไม่มี error |
| พรีวิวสด `/engine/live/` ในหน้าต่าง Electron | `readyState 4` · 1920×1080 · เดินถึง 7.25 วิ · ไม่มี error |
| ปิดแอปตอน render วิ่งอยู่ | 1 sidecar + 1 render + 3 ffmpeg → **เหลือ 0 ทุกตัว** |
| `SIGTERM` ให้เอนจินตรง ๆ ตอน render วิ่งอยู่ | เหลือ 0 ทุกตัวเช่นกัน |
| เรนเดอร์ไทยด้วย Noto Sans Thai ผ่าน `fontsdir` | สระ/วรรณยุกต์ตรงตำแหน่ง (ดูภาพที่เรนเดอร์จริง) |
| `electron-builder` → `.app` แล้วรันจากตัวที่ package | เปิดหน้าต่างได้ · เสิร์ฟ `ui/` จาก Resources · ปิดสะอาด |
| โหลดโมเดล whisper (จำลองด้วยไฟล์เล็ก) | แถบความคืบหน้า · `.part` → replace · เทียบ Content-Length |

**เกณฑ์ที่ยังไม่ผ่าน:** ฟุตเทจชุดเดียวกันบน Mac กับ Windows ได้ `edl.json` ตรงกัน —
ยังไม่มีเครื่อง Windows ให้ทดสอบ

### เฟส 2 · 3 — iOS: ⊘ ตัดออกจากขอบเขต

การวิเคราะห์ยังอยู่ที่ [ข้อ 7](#7-ทำไม-ios-ถึงเป็นคนละเรื่อง) และ
[ข้อ 5 ทาง D/E](#5-ห้าทางที่เป็นไปได้) — เก็บไว้ในฐานะ**บันทึกว่าทำไมถึงไม่ทำ**
ไม่ใช่แผนงาน ถ้าโจทย์กลับมา เริ่มอ่านที่ [ข้อ 4.6](#46-ที่ยังเหลือ--hls-จำเป็นก่อนขึ้นเฟส-2)
(HLS) ซึ่งเป็นสิ่งที่บล็อกทุกอย่างที่เหลือ

---

## 9. ข้อสรุปที่ต้องพูดให้ตรง

| คำถาม | คำตอบ |
|---|---|
| ทำเป็นแอป Mac ได้ไหม | **ทำแล้ว** — `cd desktop && npm start` เปิดได้เลยวันนี้ · `.app` ที่ package แล้วก็รันได้ · เหลือ signing |
| ทำเป็นแอป Windows ได้ไหม | **โค้ดพร้อมแล้ว** — 8 จุดแก้ครบ รวมไว้ที่ [host.py](vcut_engine/host.py) · เหลือทดสอบบนเครื่องจริงกับวาง `bin/` |
| หน้าเว็บ Next.js ทำให้ยากขึ้นไหม | **ไม่** — เป็น build step ไม่ใช่ runtime แอปที่ส่งถึงผู้ใช้ไม่ต้องมี Node.js · แก้ 4 จุด (N1–N4) เสร็จหมดแล้ว |
| ใช้ตัวห่อตัวไหน | **Electron** — ทดสอบแล้วว่า WKWebView (Tauri/pywebview/Wails บน Mac) เล่นสตรีมพรีวิวของเอนจินไม่ได้ ดู [ข้อ 4](#4-ผลทดสอบจริง-และการเลือกตัวห่อ) |
| ประหยัด 140 MB ด้วย Tauri ไม่คุ้มเหรอ | **ไม่** — แอปนี้โหลด whisper model 1.5 GB ตอนเปิดครั้งแรกอยู่แล้ว 140 MB ไม่ใช่ราคาที่ควรจ่ายด้วยฟีเจอร์หลัก |
| ทำเป็นแอป iOS ได้ไหม | **ตัดออกจากขอบเขตแล้ว** — คำตอบเดิมยังจริง (แบบเดี่ยวไม่ได้ · แบบ client ได้แต่ต้องทำ HLS ก่อน) เก็บไว้เป็นบันทึกที่[ข้อ 7](#7-ทำไม-ios-ถึงเป็นคนละเรื่อง) |
| ต้องเขียนใหม่ไหม | **ไม่ต้อง — และไม่ได้เขียนใหม่จริง ๆ** หน้าเว็บใช้ต่อ 100% ไม่แตะสักบรรทัด ส่วนเอนจินแก้ประมาณ 10 ไฟล์ โดยเรื่อง OS ทั้งหมดรวมไว้ที่โมดูลใหม่ตัวเดียว ([host.py](vcut_engine/host.py)) |
| ตอนนี้ต้องทำอะไรต่อ | หาเครื่อง Windows ทดสอบ · วาง `bin/` กับ `python/` · ไอคอน · certificate — ดู[ที่เหลือจริง ๆ](#ที่เหลือจริง-ๆ--เรียงตามลำดับที่ควรทำ) |

---

## 10. เช็กลิสต์งานทั้งหมด

รวมงานที่กระจายอยู่ในข้อ 3 · 4.6 · 6 · 8 มาไว้ที่เดียว เรียงตาม **ลำดับที่ต้องทำจริง**
ไม่ใช่ตามหัวข้อ · ทุกบรรทัดชี้ไปที่ไฟล์และเลขบรรทัดที่ต้องแก้

### แผนที่ลำดับงาน — สถานะจริง

```
 ✅ A. งานร่วม (4)  ─┬─→  ✅ B. เปลือก Electron (5)  ──→  Mac  · เหลือ signing
                     │
                     └─→  ✅ C. ฝั่ง Windows (8)      ──→  Windows · เหลือทดสอบบนเครื่องจริง

 ✅ F. โหมดสำรองในหน้าเว็บ (3)  — ทำไว้ตั้งแต่รอบก่อน

 ⊘ D. HLS · E. เปิดออกนอกเครื่อง — **นอกขอบเขตแล้ว** (เป็นงานของเฟส iOS ที่ถูกตัดออก)
```

D กับ E ยังอยู่ในเอกสารเพราะ **HLS มีของแถมที่ได้ประโยชน์กับเดสก์ท็อปด้วย** คือ
seek จริงในพรีวิวสด (ดู [ข้อ 4.6](#46-ที่ยังเหลือ--hls-จำเป็นก่อนขึ้นเฟส-2)) —
ถ้าวันหนึ่งพรีวิวสดเริ่มน่ารำคาญเพราะเลื่อนหัวอ่านไม่ได้ ให้กลับมาอ่านหัวข้อนั้น
ส่วน E (auth · TLS · rate limit) ไม่ต้องทำเลยตราบใดที่ยังผูก `127.0.0.1` อย่างเดียว

---

### A · งานร่วม — ✅ เสร็จแล้ว

|  | งาน | ทำที่ไหน |
|---|---|---|
| ☑ | **N1** `serve.py` เสิร์ฟ static ของ vcut-ui เอง และรับ `/engine/*` เป็นชื่ออื่นของราก | [serve.py](vcut_engine/serve.py) — `UI_DIR` · `strip_engine()` · `_ui()`/`_ui_path()` · [next.config.ts](vcut-ui/next.config.ts) แยกสองโหมด (dev = `rewrites()` · build = `output: "export"`) |
| ☑ | **N3** `--port 0` → ระบบเลือกพอร์ตว่างให้ แล้วพิมพ์ `VCUT_PORT=<เบอร์>` ออก stdout | [serve.py](vcut_engine/serve.py) `run()` · [cli.py](vcut_engine/cli.py) `--port` |
| ☑ | **N4** เปลี่ยนโปรเจกต์ได้โดยไม่ต้องปิดแอป | `POST /api/project` + `GET /api/projects` · `switch_project()` คืนค่าเดิมกลับถ้า config ใหม่ไม่ผ่าน · รับเฉพาะไฟล์ใต้รากโปรเจกต์ |
| ☑ | whisper model โหลดตอนรันครั้งแรกลง `~/.cache/whisper/` | [listen.py](vcut_engine/listen.py) `ensure_model()` — เขียน `.part` ก่อน replace · เทียบ `Content-Length` ก่อนรับ · `[listen] download` / `model_url` ใน [default.toml](config/default.toml) |

**สิ่งที่ได้เพิ่มโดยไม่ได้อยู่ในแผนเดิม:** ทำ N1 แล้ว `/` เป็นหน้า vcut-ui ส่วน
`viewer/index.html` ย้ายไป `/viewer` · ยังไม่ได้ `next build` ก็ถอยไปเสิร์ฟหน้าเดิม
ให้แทนจอขาว · ไฟล์ใน `/_next/static/` ได้ `Cache-Control: immutable` · เสียงใน
`public/` เสิร์ฟผ่าน `_range_file` จึงเลื่อนฟังได้จริง

---

### B · เปลือก Electron — ✅ เสร็จแล้ว (เหลือ signing)

อยู่ที่ [desktop/](desktop/) · Electron 40 · เอกสารของตัวเองที่
[desktop/README.md](desktop/README.md)

|  | งาน | ทำที่ไหน |
|---|---|---|
| ☑ | spawn sidecar → อ่านพอร์ตจาก stdout → `loadURL("http://127.0.0.1:<port>/")` | [sidecar.js](desktop/sidecar.js) · อยู่บน `http://` ปกติ จึง**ไม่ได้แตะ `_guard()` เลย** |
| ☑ | `app.on("will-quit")` → ฆ่า**ทั้งต้นไม้**โปรเซส | [main.js](desktop/main.js) + [sidecar.js](desktop/sidecar.js) `stop()` — **ทดสอบจริง**: ปิดแอปตอน render วิ่งอยู่ (1 sidecar + 1 render + 3 ffmpeg) เหลือ 0 ทุกตัว |
| ☑ | เมนูบาร์ + `dialog.showOpenDialog` ผูกกับ N4 · คีย์ลัดผูกกับของเดิม | [menu.js](desktop/menu.js) — ส่งคีย์ลงหน้าเว็บด้วย `sendInputEvent` ไม่ทำตรรกะซ้ำ |
| ☑ | `electron-builder` → `.dmg` + `.exe` (NSIS) + `electron-updater` | [package.json](desktop/package.json) · `.app` ที่ build ออกมารันได้จริงแล้ว (295 MB) |
| ☑ | **ห้าม PyInstaller** — ฝัง python-build-standalone เท่านั้น | เขียนเตือนไว้ในโค้ดที่ [sidecar.js](desktop/sidecar.js) `findPython()` และใน stage.mjs |

**installer ออกมาแล้วทั้งสองตัว** (`cd desktop && npm run dist:mac | npm run dist:win`):

| ไฟล์ | ขนาด | สถานะ |
|---|---|---|
| `vcut-0.1.0-mac-arm64.dmg` | 201 MB | **ทดสอบแล้วว่าใช้ได้จริง** — รันโดยตัด `PATH` เหลือ `/usr/bin:/bin` (ไม่เห็น homebrew ไม่เห็น Python ของเครื่อง) แล้วสั่ง render ได้ · `lsof` ยืนยันว่าโปรเซสลูกใช้ `vcut.app/Contents/Resources/bin/ffmpeg` ที่ bundle ไป |
| `vcut-0.1.0-win-x64.exe` | 180 MB | build จาก macOS ได้ · ไบนารีข้างในเป็น PE32+ x86-64 ถูกต้อง · **ยังไม่ได้ทดสอบรันบน Windows จริง** |

ของที่ต้อง bundle ดึงอัตโนมัติด้วย [scripts/fetch_vendor.py](scripts/fetch_vendor.py)
ลง `vendor/<target>/` แล้ว [stage.mjs](desktop/scripts/stage.mjs) เลือกให้ตรงข้าง
พร้อมตรวจซ้ำ — ลอกผิดข้าง = build ผ่านแต่พังตอนผู้ใช้เปิดเท่านั้น จึงต้องมีด่านนี้

**ที่ยังไม่ได้ทำเพราะรอของ:**

| เรื่อง | ติดอะไร |
|---|---|
| code signing + notarization | ยังไม่มี Developer ID certificate — ผู้ใช้ต้อง `xattr -dr com.apple.quarantine` (Mac) หรือกด "Run anyway" (Windows) ไปก่อน |
| ไอคอนแอป | ยังใช้ไอคอนตั้งต้นของ Electron — วาง `desktop/build/icon.icns` / `icon.ico` แล้วมันหยิบเอง |
| auto-update | `electron-updater` ติดตั้งแล้วแต่ยังไม่ได้ต่อ publish provider |

---

### C · ฝั่ง Windows — ✅ แก้ครบ 8 จุด

**ทุกข้อรวมไว้ที่โมดูลเดียว: [vcut_engine/host.py](vcut_engine/host.py)** — ถ้าปล่อย
ให้แต่ละโมดูลเช็ก `sys.platform` เอง มันจะกลายเป็นสิบกว่าที่ที่ไม่มีใครไล่ครบเวลา
เพิ่มแพลตฟอร์ม

|  | # | เดิม | แก้เป็น |
|---|---|---|---|
| ☑ | 1 | `h264_videotoolbox` ตายตัว | `host.pick_vcodec()` — `[encode] vcodec = "auto"` เลือกตาม OS แล้วถอยเองถ้าเครื่องนั้นไม่มี · ผลอยู่ใน `seg_key` ด้วย ย้ายเครื่องแล้วชิ้นเก่าจึงถูกทำใหม่ ไม่ปนสองแบบในเรื่องเดียว |
| ☑ | 2 | ฮาร์ดโค้ด `/opt/homebrew/...` ตอนหา ffmpeg ที่มี `ass` | `host.FFMPEG_CANDIDATES` + `host.which()` — ของที่ bundle มาก่อน แล้วค่อยของเฉพาะ OS แล้วค่อย PATH |
| ☑ | 3 | `Sukhumvit Set` ฮาร์ดโค้ด 6 จุด (ไม่ใช่ 4) | `host.default_font()` ทุกจุด + [scripts/fetch_fonts.py](scripts/fetch_fonts.py) ดึง Noto Sans Thai/Latin ลง `fonts/` + `fontsdir` ในฟิลเตอร์ `ass` — **ทดสอบจริงด้วยการเรนเดอร์ภาพ** สระ/วรรณยุกต์ตรงตำแหน่ง |
| ☑ | 3b | *(ไม่ได้อยู่ในแผนเดิม)* โปรเจกต์ที่ทำบน Mac เก็บ **ชื่อ** ฟอนต์ลง `captions.json`/`fx.json` ไปแล้ว การเปลี่ยนค่าตั้งต้นจึงช่วยแค่ของที่สร้างใหม่ | `host.resolve_font()` แทนชื่อที่รู้ว่าเป็นของ macOS ล้วนตอนเขียนไฟล์ `.ass` — ทั้งบรรทัด `Style:` และแท็ก `\fn` รายชิ้น (ต้องทำทั้งคู่ ไม่งั้นแท็กรายชิ้นเขียนทับการแทนที่ระดับสไตล์กลับไป) · แทนเฉพาะรายชื่อที่รู้แน่ ไม่ใช่เดาจาก "หาไม่เจอ" เพราะ Windows ไม่มี `fc-list` ให้ถาม |
| ☑ | 4 | `-vf ass='{fpath}'` escape แบบ `\\` ซึ่งใช้กับ path Windows ไม่ได้ | `host.filter_path()` — เปลี่ยน `\` เป็น `/` ก่อนแล้วค่อย escape `:` ได้ `C\:/งาน/a.ass` · `host.ass_filter()` ใช้ร่วมกันทั้ง caption.py กับ finish.py |
| ☑ | 5 | `text=True` ไม่ระบุ encoding (พบ **4 จุด** ไม่ใช่ 2) | `host.TEXT_IO` = `text/encoding="utf-8"/errors="replace"` ที่ util.py · serve.py · caption.py · provider.py · และ `host.prepare_env()` ตรึง UTF-8 ให้ stdout บน Windows ด้วย |
| ☑ | 6 | `whisper-cli`/`claude`/`yt-dlp` สมมติว่าอยู่ใน PATH | `host.prepare_env()` เติม `bin/` ไว้**หน้าสุด**ของ PATH ครั้งเดียวตอนสตาร์ต ครอบคลุมทั้ง 14 จุดที่เรียก `ffmpeg` ชื่อเปล่า ๆ ด้วย · `host.tool()` สำหรับตัวที่มีช่อง config · เพิ่ม `[music] ytdlp` ที่เดิมไม่มีให้ตั้ง |
| ☑ | 7 | `proc.terminate()` ไม่ลามถึงลูกของลูก | `host.kill_tree()` — `killpg` บน POSIX · `taskkill /T /F` บน Windows · `host.popen_kwargs()` แยก process group ให้ทุกโปรเซสลูก |
| ☑ | 8 | เอกสารสอน `pkill`/`lsof`/`pgrep` | [vcut README.md](vcut%20README.md) กับ [RUN-STOP.md](RUN-STOP.md) เขียนใหม่ — `kill` ตัวเดียวพอ เพราะ `serve.py` ดัก `SIGINT`/`SIGTERM` แล้วเก็บกวาดต้นไม้ก่อนออก |

**ผลข้างเคียงที่ต้องรู้:** ข้อ 7 ย้ายโปรเซสลูกไปคนละ process group ซึ่งแปลว่า
`Ctrl-C` ไม่ลามไปถึงลูกอีกต่อไป — จึงต้องเพิ่ม signal handler ใน `serve.run()`
พร้อมกัน ไม่งั้นการแก้ข้อ 7 จะ*สร้าง*ปัญหาลูกกำพร้าบน macOS ที่เดิมไม่มี
(**ทดสอบจริง:** SIGTERM ตอน render วิ่งอยู่ → sidecar/render/ffmpeg เหลือ 0 ทุกตัว)

**เกณฑ์ว่าเสร็จ (ยังทำไม่ได้):** ต้องรันฟุตเทจชุดเดียวกันบนเครื่อง Windows จริงแล้ว
ได้ `edl.json` ตรงกันทุกชิ้น — ยังไม่มีเครื่อง Windows ให้ทดสอบ ทุกข้อข้างบนจึง
ทดสอบเฉพาะฝั่ง macOS กับด้วยการตรวจตรรกะ (`filter_path` · `pick_vcodec` ·
`default_font` ทดสอบด้วยการบังคับค่าแล้วดูผล)
(`final.mp4` จะไม่เท่ากันไบต์ต่อไบต์เพราะคนละ encoder — ไม่ใช่ข้อผิดพลาด)

---

### D · HLS — ⊘ นอกขอบเขตแล้ว (แต่มีของแถมที่คุ้มกับเดสก์ท็อป)

ไม่ต้องทำเพื่อ Mac/Windows — Chromium เล่นสตรีมปัจจุบันได้อยู่แล้ว (ทดสอบในแอปที่
ห่อเสร็จแล้ว: `readyState 4` · 1920×1080 · เล่นต่อเนื่อง ไม่มี error) เก็บไว้ที่นี่
เพราะ **ได้ seek จริงในพรีวิวสด** เป็นของแถม ถ้าวันหนึ่งเรื่องนั้นเริ่มน่ารำคาญ

|  | งาน | หมายเหตุ |
|---|---|---|
| ☐ | เปลี่ยน `live_cmd()` เป็น HLS แล้วเสิร์ฟ `.m3u8`/`.m4s` ผ่าน `_range_file` เดิม | [serve.py:296](vcut_engine/serve.py#L296) · สูตร ffmpeg อยู่ที่ [ข้อ 4.6](#46-ที่ยังเหลือ--hls-จำเป็นก่อนขึ้นเฟส-2) |
| ☐ | ฝั่งหน้าเว็บ: Safari/iOS เล่น native · Chromium เพิ่ม `hls.js` (~400 KB) | [page.tsx](vcut-ui/app/page.tsx) — โหมดสำรองที่ทำไว้แล้วยังอยู่เป็นตาข่ายรอง |
| ☐ | เก็บกวาด segment ชั่วคราว | `sweep_dir` ใน [util.py](vcut_engine/util.py) มีอยู่แล้ว |

**ของแถมที่คุ้มที่สุด:** ได้ **seek จริง** — เลิกต้องขอสตรีมใหม่ทุกครั้งที่ย้ายหัวเล่น
ซึ่งเป็นข้อจำกัดที่ [serve.py:879-882](vcut_engine/serve.py#L879-L882) ยอมรับไว้ตั้งแต่ต้น

---

### E · เปิดออกนอกเครื่อง — ⊘ นอกขอบเขต (ไม่ต้องทำ)

ทั้งหมดนี้จำเป็นเฉพาะตอนเปิดออกนอก loopback ซึ่งเป็นงานของเฟส iOS ที่ถูกตัดออก
ตราบใดที่ยังผูก `127.0.0.1` และตรวจ `Host` ทุก request เหมือนเดิม ไม่ต้องทำสักข้อ
— และแอปเดสก์ท็อปบังคับให้เหลืออินสแตนซ์เดียวอยู่แล้ว จึงไม่มีเรื่องสองหน้าต่าง
เขียน `.vcut/` ชนกันด้วย

|  | งาน | เพราะ |
|---|---|---|
| ☐ | token / auth จริง | ทุกปุ่มในหน้าเว็บ spawn โปรเซสได้ = RCE ถ้าไม่มีชั้นกั้น |
| ☐ | TLS | สตรีมวิดีโอกับ transcript วิ่งผ่านเน็ตแล้ว |
| ☐ | rate limit + งานพร้อมกันได้ทีละหนึ่ง | สองหน้าต่างเขียน `.vcut/` ชุดเดียวกัน |
| ☐ | จำกัด path ที่เสิร์ฟได้ให้แน่นกว่า `SAFE_NAME` | ของเดิมออกแบบมาสำหรับ loopback |
| ☐ | SwiftUI client — เน้นขั้น 1 (ติ๊กคลิป · หมุน · ลากสลับ) กับขั้น 3 (ลากไทม์ไลน์ · รับข้อเสนอ AI) | สองขั้นที่นิ้วทำได้ดีกว่าเมาส์ |

---

### F · ✅ ทำไปแล้ว (25 ส.ค. 2026)

|  | งาน | ที่ไหน |
|---|---|---|
| ☑ | จับ `error` ของ `<video>` แล้วขึ้นข้อความไทยตาม `MediaError.code` | [page.tsx](vcut-ui/app/page.tsx) — เดิมไม่มี `onError` เลยสักตัว |
| ☑ | โหมดสำรอง "เล่นทีละชิ้น" ผ่าน `/seg/` + อุ่นชิ้นถัดไปล่วงหน้า | เลื่อนหัวอ่านในชิ้นได้จริง ต่างจากสตรีมสด |
| ☑ | สลับโหมดอัตโนมัติ 2 ชั้น (เดา WebKit จาก UA + ถอยเองเมื่อเจอ error รหัส 4) | ไม่แตะโค้ด Python เลยสักบรรทัด |

---

### สรุปสถานะ

| แพลตฟอร์ม | กลุ่ม | สถานะ |
|---|---|---|
| **Mac** | A + B | ✅ **มี `.dmg` แล้ว** 201 MB — ทดสอบว่าพึ่งพาตัวเองครบจริง · เหลือ signing + ไอคอน |
| **Windows** | A + B + C | ✅ **มี `.exe` แล้ว** 180 MB (build จาก Mac ได้) · เหลือทดสอบบนเครื่องจริง + signing |
| **iPad / iPhone** | — | ⊘ ตัดออกจากขอบเขต |

### ที่เหลือจริง ๆ — เรียงตามลำดับที่ควรทำ

```
1. เอา vcut-0.1.0-win-x64.exe ไปติดตั้งบนเครื่อง Windows จริง แล้วรันฟุตเทจ
   ชุดเดียวกัน เทียบ edl.json กับที่ได้บน Mac      ← เกณฑ์เดียวที่ยังไม่ผ่าน
2. ไอคอนแอป → desktop/build/icon.icns กับ icon.ico
3. Developer ID certificate → notarize (Mac) · code signing (Windows)
4. ต่อ electron-updater เข้ากับที่เก็บไฟล์อัปเดต
```

คำสั่ง build ทั้งหมดอยู่ที่ [RUN-STOP.md ข้อ 0](RUN-STOP.md)

### สามอย่างที่ห้ามลืม

1. **ห้าม freeze เอนจินด้วย PyInstaller** — `serve.py` spawn ตัวเองด้วย
   `sys.executable` อยู่ 4 จุด ทุกปุ่มที่สั่งงานจะพังพร้อมกันหมด
2. **`npm run build` ใน `vcut-ui/` ก่อนเสมอ** — ทั้งตอน dev และตอน package
   `serve.py` เสิร์ฟจาก `vcut-ui/out/` ไม่มีของก็ถอยไปหน้าเดิมให้
3. **whisper model 1.5 GB ห้ามใส่ installer** — โหลดตอนใช้ครั้งแรกลง
   `~/.cache/whisper/` (ทำแล้วที่ `listen.ensure_model`)
