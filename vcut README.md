# vcut

ตัดต่อวิดีโออัตโนมัติจากฟุตเทจดิบทั้งโฟลเดอร์ ควบคุมทุกอย่างด้วยไฟล์ config

ออกแบบมาให้ **ไม่มี AI อยู่ในเอนจิน** — ทุกขั้นเป็น ffmpeg/whisper ล้วน คาดเดาได้
และให้ผลเดิมทุกครั้ง ส่วน AI เป็นชั้นบนที่เรียกเครื่องมือนี้อีกที (Phase 4)

```
ต้องมี:  ffmpeg + ffprobe  ·  Python 3.11+  ·  whisper.cpp (เฉพาะขั้นถอดเสียง)
ไม่ต้องลง dependency ใด ๆ — ใช้ stdlib ล้วน
```

---

## เริ่มใช้

```bash
./vcut run -c projects/phu-soi-dao.toml     # ทำครบตั้งแต่อ่านคลิปถึง final.mp4
./vcut info -c projects/phu-soi-dao.toml    # ดูสถานะโปรเจกต์
./vcut presets                              # ดูสูตรที่มี
```

---

## ขั้นตอน

แต่ละขั้นอ่านไฟล์ที่ขั้นก่อนหน้าเขียนไว้ ไม่มีสถานะซ่อนอยู่ที่ไหน
รันขั้นไหนซ้ำก็ได้ ขั้นที่ทำไปแล้วจะข้ามจาก cache

| คำสั่ง | อ่าน | เขียน | ทำอะไร |
|---|---|---|---|
| `vcut scan` | ฟุตเทจ | `.vcut/manifest.json` | ffprobe + motion + ความสว่าง + ความดัง ทุกคลิป |
| `vcut listen` | manifest | `.vcut/transcript.json` | แยกเสียง 16k → whisper.cpp → คำพูดพร้อมเวลา |
| `vcut thumbs` | manifest | `.vcut/thumbs/` | ภาพตัวอย่าง + contact sheet 5×5 |
| `vcut ai` | manifest + transcript + sheets | `.vcut/ai.json` | ถาม AI: แบ่งบท · ให้คะแนนช็อต · ช่วงที่ควรเก็บ |
| `vcut silence` | manifest + transcript | `.vcut/silence.json` | ฟังคลื่นเสียงหาช่วงที่ไม่มีคนพูด (ใช้ตัดชน) |
| `vcut prepare` | manifest + transcript (+ ai.json) | `.vcut/pool.json` | **ขั้น 2** · ดูทีละคลิป — แยกพูด/วิว ตัดเอาช่วงที่ใช้ได้ |
| `vcut compose` | pool | `.vcut/edl.json` | **★ ขั้น 3** · หยิบจากคลังมาเรียงเป็นหนัง (7 วิธี) |
| `vcut decide` | manifest + transcript | `.vcut/edl.json` | ทำ prepare + compose รวดเดียว (คำสั่งเดิม) |
| `vcut render` | edl | `.vcut/segments/` | ตัด + แก้ภาพ/เสียงในพาสเดียว (cache ด้วย content hash) |
| `vcut assemble` | segments | `final.mp4` | ต่อเป็นไฟล์เดียว ไม่เข้ารหัสซ้ำ |
| `vcut review` | edl + render | `.vcut/review.json` | ให้ AI ดูหนังที่ตัดแล้ว → เสนอ ตัดออก/สลับที่ · ตัดช่วงเงียบ · เพลง · เสียงเอฟเฟกต์ · สติกเกอร์ · ข้อความ |
| `vcut caption` | render + captions | `final-text.mp4` | **ขั้น 4** · เขียนซับ/ข้อความลงในภาพ (ไฟล์ตัวที่สอง) |
| `vcut fx` | render + captions + fx | `final-fx.mp4` | **ขั้น 5** · แต่งหนังทุกชั้นในพาสเดียว (ไฟล์ตัวที่สาม) |
| `vcut variants` | manifest + transcript | `.vcut/variants/<id>/` | ตัด 6 แบบ (30/45/60 วิ · ตัดชิด · AI · ยิงรัว) จาก cache เดียว · `--activate` สลับแบบ — ดู [docs/QUICK-CUT-ENGINE.md](docs/QUICK-CUT-ENGINE.md) |
| `vcut autofx` | edl + transcript | `fx.json` · `captions.json` | วางชั้นแต่งตามสไตล์ให้เอง (HOOK · การ์ดปิด · ซับ · เพลง · ยิงรัว) ทุกชิ้น id `auto-*` |
| `vcut view` | ทุกอย่างข้างบน | `edl.json` | เปิดหน้าเว็บในเครื่อง ดู/แก้ EDL ด้วยตา |
| `vcut gc` | — | — | ลบ segment ที่ EDL ปัจจุบันไม่ได้ใช้ (รวม cache ของขั้น 5) |

**สามไฟล์ผลลัพธ์ อยู่ครบพร้อมกันเสมอ** — ขั้น 4 กับขั้น 5 ไม่ได้เขียนทับของขั้น 3
และไม่ได้เขียนทับกันเอง เปิดเทียบกันได้ทันทีว่าชั้นไหนทำให้อะไรเปลี่ยน

```
final.mp4        ขั้น 3 · ภาพ+เสียงล้วน                    (concat -c copy)
final-text.mp4   ขั้น 4 · + ซับ/ข้อความ                     (เข้ารหัสภาพ 1 รอบ)
final-fx.mp4     ขั้น 5 · + แอนิเมชัน ภาพซ้อน สโลว์โม เพลง   (เข้ารหัสภาพ 1 รอบ)
```

**ทั้งสามไฟล์ไปนอนที่ `~/Movies/vcut/<ชื่อโปรเจกต์>/`** ไม่ใช่โฟลเดอร์ที่รันคำสั่ง —
สั่งจากเทอร์มินัลหรือกดปุ่มในหน้าเว็บก็ได้ไฟล์ที่เดิมเสมอ และพอทำเสร็จ Finder จะ
เปิดโชว์ไฟล์ให้เลย  เปลี่ยนที่/ปิดการเด้งได้ที่ไฟล์โปรเจกต์:

```toml
[project]
out_dir = "~/Movies/vcut"       # โฟลเดอร์ผลงาน (มีโฟลเดอร์ย่อยตามชื่อโปรเจกต์)
out     = "final.mp4"           # ใส่ path เต็มแทนได้ แล้วจะไม่สนใจ out_dir
reveal  = true                  # false = ไม่ต้องเปิด Finder ให้
```

⚠️ **ขั้น 5 ไม่ได้ต่อจากขั้น 4** — มันอ่าน *ข้อมูลข้อความ* (`captions.json`) ไม่ใช่
*ไฟล์ที่มีข้อความอยู่แล้ว* ตัวหนังสือที่ถูกเผาลงภาพไปแล้วทำให้เคลื่อนไหวไม่ได้อีก
ทั้งสองขั้นจึงเป็นสาขาคู่ขนานที่แตกจากขั้น 3 คนละเส้น

### `edl.json` คือสัญญากลาง

ไฟล์นี้คือหัวใจของทั้งระบบ — เป็น JSON ธรรมดาที่บอกว่าจะเอาช็อตไหน
ช่วงวินาทีไหน เรียงลำดับใด แก้ด้วยมือได้ แล้ว `render` / `assemble` ทำตามทันที

```
เปลี่ยนลำดับ / ลบช็อตออก   →  render ไม่ทำงานเลย (วัดจริง 0.1 วินาที)
เปลี่ยนความยาว B-roll      →  render ใหม่เฉพาะชิ้นวิว
เปลี่ยน preset ทั้งชุด      →  render ใหม่เฉพาะชิ้นที่พารามิเตอร์เปลี่ยนจริง
```

วัดจริงกับฟุตเทจชุดนี้ (220 ชิ้น · 22.9 นาที · 3.1 GB):

| งาน | เวลา |
|---|---|
| `render` ครั้งแรก | ~40 นาที |
| `render` หลังแก้ EDL (ไม่แตะพารามิเตอร์ภาพ/เสียง) | **0.1 วินาที** |
| `assemble` ต่อไฟล์ใหม่ทั้งเรื่อง | ~56 วินาที (คอขวดที่การเขียนดิสก์ 3.1 GB) |

commit `edl.json` เข้า git ได้ → ผลลัพธ์ reproduce ได้ 100%

---

## ปรับค่า

ทับค่าได้ 3 ทาง เรียงจากอ่อนไปแข็ง:

```bash
# 1) preset
./vcut decide -c hiking-vlog

# 2) ไฟล์โปรเจกต์ที่ extends preset
./vcut decide -c projects/phu-soi-dao.toml

# 3) ทับทีละค่าตอนสั่ง
./vcut decide -c projects/phu-soi-dao.toml --set broll.run_max=4 --set talk.min_shot=6
```

`./vcut config -c <ไฟล์>` แสดงค่าที่ merge เสร็จแล้วทั้งหมด

### ค่าที่ปรับบ่อย

| กลุ่ม | คีย์ | ความหมาย |
|---|---|---|
| `[talk]` | `gap_merge` | ช่องเงียบสั้นกว่านี้ไม่ตัด — สูงขึ้น = ตัดถี่น้อยลง |
| | `min_shot` | ช็อตพูดสั้นสุด กันตัดจนฟังไม่รู้เรื่อง |
| `[broll]` | `motion_bands` / `durations` | ภาพยิ่งสั่นยิ่งให้สั้น |
| | `run_max` | วิวติดกันได้ไม่เกินกี่ชิ้น |
| | `drop_above_motion` | ทิ้งคลิปที่สั่นเกินค่านี้ (0 = เก็บหมด) |
| `[select]` | `target_minutes` | ตัดให้เหลือกี่นาที (0 = ไม่จำกัด) |
| `[video]` | `vertical_mode` | `blur_pad` / `pillarbox` / `crop` |
| `[audio]` | `target_lufs_talk` | ความดังเป้าของช่วงพูด |
| | `compressor` / `denoise` | เปิด/ปิดการแต่งเสียง |
| | `allow_limit` | ยอมให้ limiter กดยอดคลื่นได้กี่ dB |

---

## สูตรที่มี

ตัวเลขด้านล่างวัดจริงจากฟุตเทจ 273 คลิป / 51.1 นาที

| preset | ผลที่ได้ | จุดเด่น |
|---|---|---|
| `hiking-vlog` | **220 ชิ้น · 22.9 นาที** (116 พูด + 104 วิว) | ใช้ทุกคลิป เสียงไม่แต่ง |
| `v2-classic` | **194 ชิ้น · 26.6 นาที** (116 พูด + 78 วิว) | คัดคลิปสั่นทิ้ง 60 คลิป ทุกช็อตยาว 6 วิเท่ากัน |
| `story-cut` | **81 ชิ้น · 10.8 นาที** (45 พูด + 36 วิว) | คัดช็อตที่ฟังรู้เรื่อง + ภาพนิ่ง ตามเป้าความยาว |
| `vertical-short` | หนังแนวตั้ง 9:16 · ตัดชิด · เป้า 45 วิ | ฐานของสูตร TikTok ทั้งสี่ข้างล่าง |
| `tiktok-sell` | A · ปิดการขาย / แนะนำช่อง · ช็อต 1.7–2.0 วิ | ต่อยอด `vertical-short` — ตัวเลขจาก [tiktok-reference](docs/tiktok-reference/README.md) คลิป 01–03 |
| `tiktok-proof` | B · โชว์หลักฐาน · ช็อต 2.4 วิ | คลิป 04 — ความดังตรงเป้า TikTok พอดี |
| `tiktok-teach` | C · สอนกรอบวิธีคิด · ช็อต 5.0 วิ | คลิป 05 — เน้นเสียงพูด ตัดถี่น้อย |
| `tiktok-compare` | D · Before \| After · ช็อต 1.9–2.8 วิ | คลิป 06–07 — ฝั่งตัดเท่านั้น (คำสั่ง compare ถูกถอดแล้ว) |

⚠️ `target_minutes` เป็นเพดาน ไม่ใช่การรับประกัน — ถ้า B-roll ที่ผ่านตัวกรอง
มีไม่พอ ผลจะสั้นกว่าเป้า ปรับได้ที่ `select.talk_ratio` (สูงขึ้น = ใช้ช่วงพูดมากขึ้น)
หรือคลาย `broll.drop_above_motion` / `broll.drop_below_bright`

---

## เรื่องที่เอนจินจัดการให้อัตโนมัติ

ทั้งหมดนี้แก้ในพาสเดียวตอน render — ไม่มีขั้น normalize แยก จึงเข้ารหัสรอบเดียว
ภาพคมกว่าและประหยัดดิสก์

| ปัญหา | วิธีแก้ |
|---|---|
| metadata rotation โกหก (คนนอนขวางจอ) | `[scan.rotation_overrides]` บังคับ transpose รายคลิป |
| คลิปกลับหัว 180° | ffmpeg autorotate |
| คลิปแนวตั้งในจอ 16:9 | เต็มความสูง + พื้นหลังเบลอ (ย่อ 480×270 ก่อนเบลอ เร็วกว่าหลายเท่า) |
| HEVC full range สีจัดกว่าคลิปอื่น | `[scan.color] full_range_clips` → แปลงเป็น tv range |
| fps ผสมหลายแบบ / VFR | บังคับ CFR 59.94 — ถ้าข้ามขั้นนี้เสียงจะเลื่อนสะสมจนปากไม่ตรง |
| เสียง mono ปนอยู่คลิปเดียว | อัปเป็น stereo ให้เท่ากันหมด |
| คลิปไม่มีเสียงเลย | ใส่ track เงียบให้ ไม่งั้น concat พัง |
| เสียงต้นฉบับเบามาก (−50 LUFS) | คำนวณ gain แบบรู้พีค แล้วให้ limiter แตะเท่าที่จำเป็น |

### ⚠️ บั๊ก `alimiter` ที่ต้องระวัง

`alimiter` ของ ffmpeg เปิดออปชัน `level` ไว้โดยดีฟอลต์ ซึ่ง **auto-normalize
สัญญาณขึ้นชนเพดานเสมอ ทับค่า gain ที่คำนวณมาทั้งหมด** ทำให้ true peak ทะลุ 0 dBFS
แม้ตั้ง limiter ไว้ต่ำแล้ว

vcut ตั้ง `level=disabled` ให้อัตโนมัติ (`[audio] limiter_level_disabled = true`)
อย่าปิดค่านี้ถ้าไม่รู้ว่ากำลังทำอะไร

### การคำนวณ gain

```
gain = min( เป้าความดัง − ความดังจริง ,  (เพดานพีค + allow_limit) − พีคจริง )
```

ถ้าดูความดังอย่างเดียว คลิปที่เบามากจะถูกเร่งจนพีคทะลุ แล้วโยนภาระให้ limiter
บีบจนเสียงแบน สูตรนี้กันไม่ให้ไปถึงจุดนั้นตั้งแต่แรก — `allow_limit` คือปุ่ม
แลกระหว่าง "เสียงสม่ำเสมอ" กับ "ไดนามิกเดิม"

| `allow_limit` | ช่วงกว้างของเสียงพูด | ชิ้นที่ limiter แตะ |
|---|---|---|
| 0 | 18.7 dB (ดัง-เบาสวิงมาก) | 0 |
| 4 | 14.7 dB | 73 |
| 8 | 10.7 dB | 30 |
| **12** (ดีฟอลต์) | **6.7 dB** | **11** |

---

## AI — ชั้นที่ปรึกษา ไม่ใช่ชั้นที่ลงมือ

กฎในเอนจินคัดช็อตได้ แต่ไม่รู้ว่าเรื่องเล่าถึงไหน — งานแบบ "เล่าตามลำดับการเดินทาง"
หรือ "เอาช่วงที่พูดวนซ้ำออก" ต้องมีคนอ่านเนื้อหาจริง

```bash
./vcut thumbs -c story-ai                 # AI ต้องมีภาพดูก่อน
./vcut ai -c story-ai --goal "ตัดเหลือ 10 นาที เล่าตามลำดับการเดินทาง"
./vcut decide -c story-ai                 # อ่าน ai.json — ไม่เรียก AI ซ้ำ
```

**AI เขียนได้ไฟล์เดียวคือ `.vcut/ai.json`** ซึ่งเป็นความเห็นล้วน ๆ:

```
chapters[]        แบ่งบท + ลำดับคลิปในบท
clips[].score     คะแนนความน่าเก็บ 0–1
clips[].keep      ช่วงวินาทีที่ควรเก็บ
clips[].drop      ควรทิ้งทั้งคลิป
```

`decide.py` เอาไปใช้ด้วยกฎที่เขียนตายตัวใน `[ai.apply]` ตัว AI ไม่เคยแตะ `edl.json` เอง

| คีย์ | ทำอะไร |
|---|---|
| `order` | ให้บทที่ AI แบ่งเป็นตัวจัดลำดับ — **เฉพาะตอน `[order] mode = "pick"`** |
| `drop` | ทิ้งคลิปที่ AI บอกว่าใช้ไม่ได้ |
| `trim` | ช่วงพูด: ตัดร่วมกับ VAD · ช่วงวิว: AI เลือกตำแหน่ง **แต่กฎยังคุมความยาว** |
| `score_weight` | น้ำหนักคะแนน AI ตอน `[select]` คัดช็อต (0 = กฎล้วน · 1 = AI ล้วน) |

บทที่ AI แบ่งนับเป็น **วิธีเรียงอีกแบบที่ซ่อนอยู่ใต้ `pick`** ไม่ใช่ค่าที่มาทับตัวเลือก
ของคน — ตั้ง `[order] mode` เป็นอย่างอื่น (`stage1` · `date` · …) แล้วสิ่งที่เลือกไว้
ชนะเสมอ พร้อมขึ้นคำเตือนบอกว่าบทที่ AI แบ่งไว้ไม่ได้ถูกใช้

ผลที่ได้จึงยังคาดเดาได้:

```
รัน decide ซ้ำกี่ครั้งก็ไม่เสียเงินเรียก AI ใหม่   →  ปรับสูตรได้ฟรีไม่จำกัด
commit ai.json เข้า git                        →  คนอื่นได้ผลเดียวกัน ไม่ต้องมี API key
--set ai.enabled=false                         →  กลับไปเป็นผลของ Phase 3 เป๊ะ ๆ
```

AI เห็นแค่ **ข้อความ + contact sheet 11 ภาพ** — ไม่มีการส่งไฟล์วิดีโอออกไปไหน
ทุกอย่างที่ส่งไปเก็บไว้อ่านย้อนหลังได้ที่ `.vcut/ai/<task>.prompt.md`

### เลือกได้ว่าจะใช้ AI เจ้าไหน — `[ai] provider`

| ค่า | คุยยังไง | ต้องมีอะไร | เห็นภาพไหม |
|---|---|---|---|
| `claude_cli` (ตั้งต้น) | เรียก `claude -p` ในเครื่อง | Claude Code ที่สมัครไว้ | เห็น (อ่านไฟล์ในเครื่องเองได้) |
| `gemini` | ยิง HTTP ไป Generative Language API | API key | ยังไม่เห็น (ข้อความล้วน) |

สลับได้ทั้ง `vcut ai` และ `vcut review` พร้อมกันด้วยคีย์เดียว (จะให้คนละเจ้าก็ตั้งแยกที่
`[review] provider`) — ทั้งสองทางเข้าฟังก์ชันเดียวกันที่ `provider.ask()` โมดูลอื่นจึงไม่
ต้องรู้ว่าข้างในต่างกันยังไง  Gemini ไม่มีเครื่องมือเขียนไฟล์ ระบบจึงต่อท้ายโปรมป์บอกให้
ตอบ JSON กลับมาทางข้อความแทน และบังคับ `responseMimeType: application/json` ให้อีกชั้น

**API key ไม่ได้อยู่ในไฟล์โปรเจกต์โดยตั้งใจ** — ไฟล์นั้นถูก commit ลง git
อ่านจาก `GEMINI_API_KEY` (หรือ `GOOGLE_API_KEY`) ก่อน ไม่มีค่อยอ่าน `.vcut/secrets.json`
ซึ่งอยู่ใน `.gitignore` อยู่แล้วและถูกตั้งสิทธิ์ `0600` ให้ · ใส่ผ่านหน้าเว็บได้ที่แท็บ AI
และไม่มีเส้นทางไหนส่งค่า key กลับออกไปที่หน้าเว็บอีก

### AI สองบทบาท คนละตำแหน่งในไปป์ไลน์

```
vcut ai      อยู่ก่อน decide    เห็นกองคลิปดิบ 273 อัน   → .vcut/ai.json
vcut review  อยู่หลัง render    เห็นหนังจริงที่ตัดแล้ว   → .vcut/review.json
```

ตัวหลังได้เปรียบตรงที่เห็น **ลำดับจริงที่คนดูจะเจอ** — บทพูดเรียงตามที่จะได้ยินจริง
ความยาวแต่ละช็อตจริง เวลาในหนังจริง เสียงที่ปรับแล้วจริง จึงตอบเรื่อง *"ตรงไหนยืดเยื้อ"*
หรือ *"ช็อตนี้พูดซ้ำกับช็อตที่ 12"* ได้ ซึ่งตอนอยู่ก่อน `decide` ตอบไม่ได้เลย

```bash
./vcut review -c projects/phu-soi-dao.toml --context "ช่วงกลางเรื่องยืดไป"
```

**สั่งได้ 6 งาน ติ๊กเลือกทีละรอบ** — `--task` ซ้ำได้ ไม่ระบุ = ตาม `[review] tasks`

| งาน | ทำอะไร | ผลลงไฟล์ | ต้องตัดวิดีโอใหม่ไหม |
|---|---|---|---|
| `cut` | เอาช็อตออก · สลับลำดับ | `edl.json` | ไม่ |
| `trim` | ตัดช่วงเงียบหัว-ท้ายช็อต | `edl.json` | เฉพาะชิ้นที่ตัด (~วินาที) |
| `music` | เลือกเพลงจากคลังมาวาง | `fx.json` | ไม่ |
| `sfx` | วางเสียงเอฟเฟกต์ | `fx.json` | ไม่ |
| `sticker` | วางสติกเกอร์ / ภาพซ้อน | `fx.json` | ไม่ |
| `text` | เขียนข้อความบนหนัง | `fx.json` | ไม่ |

```bash
./vcut review -c <preset> --task cut --task trim      # งานฝั่งไทม์ไลน์
./vcut review -c <preset> --task music --task text    # งานฝั่งแต่งหนัง
```

`sfx` กับ `sticker` **สั่งจากหน้าเว็บเท่านั้น** — รายการตัวอย่าง (เสียง 60 · สติกเกอร์ 134)
อยู่ใน `vcut-ui/public/` ซึ่งเอนจินไม่รู้จัก หน้าเว็บจึงส่งแคตตาล็อกมาให้พร้อมคำสั่ง
แล้ว AI เลือกได้เฉพาะไฟล์ในรายการนั้น — ตอบชื่อไฟล์ที่ไม่มีจริงมา ข้อนั้นถูกทิ้ง

**ตัวเลขของ `trim` ไม่ได้มาจากการเดาของ AI** — เอนจินอ่าน `.vcut/silence.json` ที่วัด
ด้วย `ffmpeg silencedetect` ไว้แล้ว คำนวณว่าหัว/ท้ายช็อตไหนตัดได้กี่วินาที (เผื่อ
`[jumpcut] pad` · ไม่ตัดจนช็อตสั้นกว่า `review.trim_keep`) แล้วส่งเป็น *ตัวเลือกพร้อม id*
ให้ AI คัด — AI ตอบได้แค่ id จึงไม่มีทางเสนอเวลาที่ไม่มีอยู่จริง  ช่องเงียบ *กลาง* ช็อต
ไม่อยู่ในนี้ (ต้องผ่าช็อตเป็นสองชิ้นแล้วเลขลำดับของข้ออื่นจะเลื่อนหมด) — อันนั้นเป็นงาน
ของ `[jumpcut]` ที่ขั้น 2 ซึ่งตัดตั้งแต่ตอนสร้างคลัง

หลักเดิมยังอยู่ทั้งหมด: **AI เสนอ คนกดรับ** `review.json` เป็นข้อเสนอล้วน ไม่มีบรรทัดไหน
แตะ `edl.json` หรือ `fx.json` เอง — หน้าเว็บลงมือให้ตอนกดรับ แล้วยังต้องกดบันทึกอีกที

ทำงานผ่านหน้าเว็บเป็นหลัก — ติ๊กว่าจะให้ทำงานไหน พิมพ์บริบท กดวิเคราะห์ แล้วได้
ข้อเสนอเป็นการ์ดให้ **กดรับทีละข้อ หรือ "รับทั้งหมด"** แล้วของขยับให้เห็นก่อน
ค่อยกดบันทึก — ไม่มีเส้นทางเขียน EDL/FX เส้นใหม่ ใช้ปุ่มบันทึกเดิมทุกประการ

"รับทั้งหมด" คิดทั้งชุดในการแก้ครั้งเดียว ไม่ใช่กดทีละข้อรัว ๆ — ลบช็อตแล้วเลขลำดับ
ของข้อถัดไปเลื่อน และของชั้นแต่งหนังทุกชิ้นอ่าน draft ก้อนเดียวกัน ถ้าใส่ทีละชิ้น
ชิ้นหลังจะทับชิ้นแรกหายเงียบ ๆ (ทุกข้อจึงติดเลขลำดับ "ของตอนที่ AI ดู" ไว้กับตัว)

ถ้า EDL ถูกแก้หลังจาก AI ดู ตำแหน่งที่มันอ้างจะเลื่อน — ระบบเทียบลายนิ้วมือของ EDL
แล้ว**ปิดปุ่มรับทั้งหมด**พร้อมบอกให้วิเคราะห์ใหม่ ไม่ปล่อยให้กดจนตัดผิดช็อต

### พิมพ์ในแชท Claude Code ก็ได้

`.claude/skills/video-edit/SKILL.md` สอน Claude Code ให้แปลคำสั่งภาษาไทยเป็น
คำสั่ง `vcut` ให้เอง — พิมพ์ว่า *"ตัดเหลือ 10 นาที เล่าตามลำดับการเดินทาง"*
หรือ *"วิวติดกันเยอะไป"* แล้วมันจะเลือกคีย์ config ที่ถูกให้

---

## หน้าเว็บ — `vcut view`

```bash
./vcut view -c projects/phu-soi-dao.toml        # http://127.0.0.1:8765
```

หน้าเดียวจบ 3 ขั้น เสิร์ฟด้วย stdlib ล้วน — ไม่มี npm ไม่มี build step ไม่มี DB
ไม่มี login ผูกกับ `127.0.0.1` เท่านั้นและตรวจ `Host` ทุก request เครื่องอื่นใน
วงแลนต่อไม่ได้ (`/setup` ของเดิมถูกยุบเข้ามาที่นี่แล้ว — เข้าไปจะเด้งกลับหน้าเดียว)

### เปิด · ปิด · เช็คว่ารันอยู่ไหม

```bash
./vcut view -c projects/phu-soi-dao.toml               # เปิดเบราว์เซอร์ให้เอง
./vcut view -c projects/phu-soi-dao.toml --no-open     # ไม่ต้องเปิดเบราว์เซอร์
./vcut view -c projects/phu-soi-dao.toml --port 8766   # ค่าตั้งต้นคือ 8765
```

มันยึดเทอร์มินัลไว้และพิมพ์ URL ให้ — **`Ctrl-C` เพื่อปิด** อยากปล่อยไว้เบื้องหลัง

```bash
(./vcut view -c projects/phu-soi-dao.toml --no-open > /tmp/vcut-view.log 2>&1 &)
```

เช็คว่ายังรันอยู่ไหม

```bash
pgrep -fl "vcut view"                       # pid + คำสั่งเต็ม
lsof -nP -iTCP:8765 -sTCP:LISTEN            # ใครจับพอร์ตนี้อยู่
curl -s http://127.0.0.1:8765/api/state | head -c 120
```

สั่งปิด

```bash
pkill -f "vcut view"      # ปิดทุกตัว
kill <pid>                # ปิดเฉพาะตัวที่ต้องการ
```

**ปิดยังไงมีผลกับงานที่ค้างอยู่** — ทุกปุ่มในหน้าเว็บ spawn เป็นโปรเซสลูก
(`serve.Job` → `subprocess.Popen` ไม่ได้แยก session)

| วิธีปิด | เซิร์ฟเวอร์ | งานที่กำลังรัน (`render` · `ai` · …) |
|---|---|---|
| `Ctrl-C` ในเทอร์มินัลที่รันอยู่ | ปิด | ปิดตามไปด้วย — SIGINT ไปทั้ง process group |
| `pkill` / `kill <pid>` | ปิด | **ยังรันต่อ** กลายเป็นลูกกำพร้าที่ไม่มีใครดู log |

ทางที่ปลอดภัยคือกด **หยุด** ในหน้าเว็บก่อน แล้วค่อยปิดเซิร์ฟเวอร์ ถ้าเผลอปิดไปแล้ว
เก็บกวาดด้วย `pgrep -fl "vcut (run|render|ai|listen)"` แล้ว `kill` ทีละตัว

### แก้โค้ดแล้วต้องรีสตาร์ตไหม

| แก้อะไร | ต้องทำอะไร |
|---|---|
| `viewer/index.html` | refresh หน้าเว็บพอ — ไฟล์ถูกอ่านสดทุก request |
| `vcut_engine/*.py` | **ต้องปิดแล้วเปิดใหม่** Python โหลดโมดูลไว้ตั้งแต่ตอนสตาร์ต |
| `.toml` ที่แก้จากหน้าเว็บ | ไม่ต้อง — `reload_ctx()` อ่านไฟล์ใหม่ให้หลังบันทึกทุกครั้ง |
| `.toml` ที่แก้ด้วยมือข้างนอก | ต้องรีสตาร์ต ไม่งั้นหน้าเว็บยังแสดงค่าเก่าที่ค้างในหน่วยความจำ |

ขึ้น `Address already in use` แปลว่ามีตัวเก่ารันอยู่แล้ว — `pkill -f "vcut view"`
ก่อนแล้วค่อยเปิดใหม่ หรือเปิดคนละพอร์ตด้วย `--port` (หลายตัวชี้โปรเจกต์เดียวกัน
อ่านได้ปลอดภัย แต่อย่าสั่งงานพร้อมกันสองหน้าต่าง มันเขียน `.vcut/` ชุดเดียวกัน)

### แท็บ 5 ขั้น

```
ขั้น 1  เลือกฟุตเทจ         scan + thumbs                     → manifest.json
ขั้น 2  เตรียมวิดีโอ        listen + ai + silence + prepare   → pool.json
ขั้น 3  รวมเป็นหนัง         compose + render + assemble       → edl.json → final.mp4
ขั้น 4  รวมเป็นหนังแบบมีText caption                          → captions.json → final-text.mp4
ขั้น 5  แต่งหนัง            finish                            → fx.json → final-fx.mp4
```

เปิดมาจะไปที่ขั้นแรกที่ยังทำไม่เสร็จให้เอง ค่าที่ใช้บ่อยอยู่หน้าการ์ด ที่เหลือพับไว้ใน
**ตั้งค่าเพิ่มเติม** · ขั้น 2 แสดงคลังแยกสองคอลัมน์ *วิดีโอพูด / วิดีโอวิว* ·
ขั้น 3 มี 7 วิธีเลือก + ไทม์ไลน์ลากสลับได้ + ให้ AI ดูหนังที่ตัดแล้ว ·
ขั้น 4 กับขั้น 5 มีพรีวิวที่ลากวางของบนภาพได้ตรง ๆ (ดูสองหัวข้อท้ายเอกสาร)

**ปุ่ม *ทำทุกขั้น* วิ่งครบขั้น 1→5** จึงได้ครบทั้ง `final.mp4` · `final-text.mp4` ·
`final-fx.mp4` ในครั้งเดียว (สองไฟล์ท้ายเข้ารหัสภาพใหม่ทั้งเรื่อง เผื่อเวลาไว้ด้วย)
อยากได้แค่ไฟล์เดียวให้กดปุ่มบนการ์ดของขั้นนั้นแทน — ปุ่มพวกนั้นทำแค่ขั้นตัวเองเสมอ
(ดู [ปุ่มผลิตไฟล์ประจำขั้น](#ปุ่มผลิตไฟล์ประจำขั้น))

### บันทึกการทำงาน — กำลังทำอยู่ไหม ถึงไหนแล้ว

กดปุ่มไหนก็ตาม แผงขวาบนขึ้นสถานะทันทีและอัปเดตทุก 0.8 วินาที

```
● กำลังดึงบทพูด…   คำสั่งที่ 1/4                      1:37
  ███████████░░░░░░░░░░░░░
  ถอดเสียง 128/284 (45%) · เหลือ ~1:12 · IMG_7156
```

จุดกะพริบ = กำลังทำ · เขียว = เสร็จ · แดง = ล้มเหลว · เทา = คนกดหยุด และปุ่มที่
กำลังรันอยู่มีเส้นกะพริบใต้ปุ่ม จะได้รู้ว่ากดอันไหนไป

ตัวเลขมาจากแถบความคืบหน้าของเอนจินเอง (`util.Progress`) — `text=True` แปลง `\r`
เป็น `\n` ให้ แต่ละครั้งที่แถบขยับจึงมาถึงเซิร์ฟเวอร์เป็นบรรทัดของตัวเอง `serve.Job`
แกะเป็นตัวเลขส่งให้หน้าเว็บวาดแถบจริง **แล้วไม่เอาบรรทัดแถบลง log** ไม่งั้นข้อความ
สรุปจะจมหายไปในแถบเป็นพันบรรทัด

ขั้นที่ไม่มีแถบก็ยังบอกได้ว่ายังไม่ตาย: `ai` รายงานเป็น *ก้อนที่ n/N* พร้อมชื่องานย่อย
(แบ่งบทเล่าเรื่อง · อ่านความหมายรายคลิป …) ส่วนขั้นที่เร็วจนไม่มีอะไรรายงานจะขึ้นแถบ
วิ่งไปมาพร้อมนาฬิกาจับเวลา ไม่ใช่ค้างเป็นหน้าจอเปล่า

### ขั้น 1 — เลือกคลิปทีละตัว

คลิปทุกตัวขึ้นครบในตารางเดียว กรองด้วย **ทั้งหมด / แนวนอน / แนวตั้ง** ติ๊กเอา-ไม่เอา
ทีละตัวหรือทั้งชุด (`เลือกทั้งหมด` · `ไม่เอาทั้งหมด` · `กลับด้าน` ทำเฉพาะที่กรองอยู่)
คลิปที่ไม่ติ๊กจะ**ไม่เข้าคลังของขั้น 2 เลย** — ต่างจากตัวกรองใน `[broll]` ที่ยังหยิบ
กลับมาใส่เองได้ในขั้น 3

กดที่ตัวคลิปเปิดกล่องที่**เล่นไฟล์ต้นฉบับได้จริง** (stream ตรงจากดิสก์ รองรับ Range
เลื่อนดูได้) พร้อมสองอย่างที่ต้องตัดสินตั้งแต่ตอนนี้:

| | |
|---|---|
| **หมุนคลิป** | ตามไฟล์ · ขวา 90° · ซ้าย 90° · กลับหัว 180° — สำหรับคลิปที่ metadata rotation โกหก |
| **แนวตั้ง → 16:9** | เลือกได้ทีละคลิปว่าจะ *เต็มความสูงด้านข้างเบลอ* · *ย่อทั้งภาพแถบดำสองข้าง* · *ครอปเต็มจอ* |

ปุ่ม **ดูของจริง** ตัด 3 วินาทีกลางคลิป render ด้วย `build_vfilter` ตัวเดียวกับตอนตัดจริง
— ที่เห็นคือสิ่งที่จะได้ ไม่ใช่ภาพจำลอง ครั้งแรกรอ ~1 วินาที หลังจากนั้นอ่านจาก cache

**ลากสลับตำแหน่งได้** — คลิกค้างที่คลิปแล้วลากไปวางตรงไหนก็ได้ เส้นสีน้ำเงินบอกว่า
จะแทรกซ้ายหรือขวาของใบที่ชี้อยู่ ลากตอนกรองแนวตั้ง/แนวนอนอยู่ก็ได้ ระบบย้ายใน
**รายการเต็ม** โดยอิงคลิปที่ปล่อยทับ ลำดับที่ได้เก็บลง `[scan] order` แล้ว
`compose.apply_order()` แปะเลข `_seq` ให้ทุกชิ้นตอนเรียงหนัง

`_seq` ไม่ไปทับ `num` — `num` คือเลขบนชื่อไฟล์ซึ่ง `compose.mode = numbers` กับ
`avoid_adjacent` ยังต้องใช้ตามความหมายเดิม ไม่มีลำดับที่จัดเอง `_seq` ก็เท่ากับ
เรียงตามเลขไฟล์พอดี **ผลลัพธ์เดิมจึงไม่ขยับสักชิ้น** (วัดแล้วกับคลัง 406 ชิ้น)

ลากแล้ว**ไม่ต้อง render ใหม่** — ลำดับไม่ได้อยู่ใน `seg_key()` ที่ตัดสินว่าชิ้นไหน
ต้องเข้ารหัสใหม่ เปลี่ยนแค่ลำดับตอน `assemble` ต่อไฟล์ · แต่ต้องกด **รวมเป็นหนัง**
ในขั้น 3 ใหม่ เพราะ EDL เขียนลำดับไว้แล้ว

⚡ **หมุนคลิปแล้วไม่ต้อง scan ใหม่** — `rot_override` มีผลแค่กับ `orient`/`dw`/`dh`
ส่วน motion · ความสว่าง · ความดัง วัดจากไฟล์ดิบก่อนหมุนทั้งหมด `clips.sync_manifest()`
จึงแก้ manifest ตรงจุดได้ถูกต้องเท่ากับ scan ใหม่ทั้งกอง แต่ใช้เวลาไม่ถึงวินาที
แทนที่จะเป็น 40 นาที (ภาพตัวอย่างของคลิปนั้นถูกจับใหม่ให้อัตโนมัติด้วย)

ทั้งสี่อย่างเก็บเป็น **config ไม่ใช่ไฟล์ cache** ลบ `.vcut/` ทิ้งทั้งโฟลเดอร์ก็ยังได้ผลเดิม:

```toml
[scan]
exclude = ["IMG_7068", "IMG_7070"]     # ไม่เอาคลิปพวกนี้
order   = ["IMG_7772", "IMG_7068", …]  # ลำดับที่ลากจัดเอง (ว่าง = ตามเลขไฟล์)

[scan.rotation_overrides]
IMG_7189 = "transpose=2"               # metadata โกหก บังคับหมุนเอง

[video.vertical_overrides]
IMG_7162 = "pillarbox"                 # คลิปนี้ย่อ ที่เหลือใช้ vertical_mode ปกติ
```

วัดจริง: ไม่เอา 3 คลิป + หมุน 1 + เปลี่ยนโหมด 1 → คลัง 234 เหลือ 231 ชิ้น และ
**render ใหม่แค่ 1 ชิ้น** เพราะ hash ของ segment เปลี่ยนเฉพาะชิ้นที่โดนจริง

`order` เขียนรายชื่อครบทั้งกองเสมอ ไม่ใช่เฉพาะตัวที่ย้าย — ลำดับจึงอ่านออกตรง ๆ
ไม่ต้องเดา และสองกรณีที่พังง่ายก็รอด: ชื่อที่ไฟล์ถูกลบไปแล้วถูกข้าม ส่วน**คลิปที่
เพิ่งก๊อปเข้าโฟลเดอร์ทีหลัง** (ยังไม่มีชื่อใน `order`) จะถูกแทรกกลับข้างเพื่อนบ้าน
ตามเลขไฟล์ ไม่ใช่ไปกองต่อท้าย — ดู `clips.arrange()`

**ลำดับตัดสินกันแค่สองที่: ขั้น 1 กับ ขั้น 3** — ขั้น 2 ไม่ได้เรียงอะไรเลย มันแค่ตัด
คลิปเป็นชิ้นเก็บใส่คลัง (`pool.json` เป็น*คลัง* ไม่ใช่*ลำดับ*) ขั้น 1 ตอบว่า
*ลำดับคืออะไร* ขั้น 3 ตอบว่า*จะเรียงด้วยเกณฑ์อะไร* แล้วผสมกันตอนกด "รวมเป็นหนัง"

### ขั้น 3 — สองแกนที่แยกจากกัน: เอาชิ้นไหน × เรียงยังไง

หน้าเว็บแยกให้เห็นชัดว่าอันไหนเสียโควตา AI อันไหนเป็นกฎล้วน แล้วเลือกลำดับต่างหาก

```
ไม่ใช้ AI  ตัดสินด้วยกฎล้วน กดกี่รอบก็ได้ ไม่เสียโควตา
  [เอาทั้งหมด] [สลับตามรูปแบบ] [กำหนดเวลารวม] [ตามเลขคลิป] [ตามช่วงเวลาถ่าย] [เลือกทีละชิ้น]
ใช้ AI     เรียกโมเดลจริงทุกครั้งที่กด — เสียโควตา
  [ให้ AI เลือก]

แล้วเรียงยังไง
  [ลำดับจากขั้น 1] [ตามที่วิธีเลือกจัดให้] [วันที่ถ่าย] [เลขที่วิดีโอ] [ความยาว] [ที่ลากไว้ในไทม์ไลน์]
```

`[order] mode` — ใช้ข้ามกับวิธีเลือกชิ้นได้ทุกคู่ (7 × 6)

| ค่า | เรียงตาม |
|---|---|
| `stage1` **(ค่าตั้งต้น)** | ลำดับที่ลากจัดไว้ในขั้น 1 — ไม่ได้ลาก = ตามเลขไฟล์ |
| `pick` | ไม่เรียงซ้ำ ปล่อยตามที่วิธีเลือกชิ้นจัดมาให้ — หรือบทที่ AI แบ่ง ถ้าเปิด `[ai.apply] order` |
| `date` | วันที่ถ่ายจริงจาก metadata |
| `number` | เลขบนชื่อไฟล์ (`IMG_7068` → 7068) ไม่สนลำดับที่ลาก |
| `duration` | ความยาวชิ้น — ใช้คู่กับ `reverse` ได้ |
| `manual` | ลำดับที่ลากไว้ในไทม์ไลน์รอบก่อน (อ่านจาก `edl.json`) ชิ้นใหม่ต่อท้าย |

ชื่อเก่ายังใช้ได้: `filename` → `stage1` · `mtime` → `date` (ดู `config.ORDER_ALIAS`)

**`manual` แก้ปัญหาที่เจ็บมานาน** — ไทม์ไลน์เขียนทับ `edl.json` ตรง ๆ กด "จัดใหม่"
ทีเดียวงานที่ลากไว้หายหมด ตั้งเป็น `manual` แล้วลำดับที่จัดไว้รอดทุกครั้ง

⚠️ **สองจุดที่วิธีเลือกกับวิธีเรียงตีกัน**

* **สลับตามรูปแบบ** ใช้ลำดับนี้ *ข้างในคิวพูดกับคิววิว* แล้วสลับประเภทตามรูปแบบ
  — เรียงทับผลลัพธ์รวมเมื่อไร การสลับหายทั้งดุ้น จึงไม่ทำ วัดแล้ว: `pattern` +
  `stage1` ได้ EDL เดิมเป๊ะทุกชิ้น ส่วน `pattern` + `date` ยังสลับ T/B/B เหมือนเดิม
  แต่เดินตามวันที่
* **เลือกทีละชิ้น / ให้ AI เลือก** ให้ลำดับมาเป็นรายการตรง ๆ ต้องตั้ง `pick` ถึงจะ
  เก็บไว้ — หน้าเว็บสลับให้อัตโนมัติตอนกดเลือกสองโหมดนี้ ถ้าฝืนตั้งอย่างอื่นเอนจิน
  เตือนก่อนเรียงทับ

**บทที่ AI แบ่งไม่ใช่แกนที่สาม** — `[ai.apply] order` เคยชิงเรียงก่อนทุกครั้งที่เปิด AI
ไว้ ตั้งแต่ยุคที่ยังไม่มีตัวเลือกลำดับในขั้น 3 พอมีแล้วมันกลืนสิ่งที่คนเลือกไปเงียบ ๆ
ตอนนี้บทของ AI จัดลำดับให้เฉพาะตอน `pick` เท่านั้น เลือกแบบอื่นไว้คนชนะเสมอ
และเอนจินเตือนว่าบทที่แบ่งไว้ไม่ได้ถูกใช้

**"ไม่ใช้ AI" แปลว่าไม่ใช้จริง** — กดโหมดในกลุ่มกฎล้วน หน้าเว็บปิด `[ai] enabled`
ให้เอง ไม่งั้นชื่อกลุ่มโกหก: คะแนนกับบทของ AI ยังถ่วงผลอยู่เงียบ ๆ ทั้งที่ปุ่มบอกว่า
กฎล้วน · เปิดกลับได้ที่สวิตช์ "ใช้ความเห็นจาก AI" ในการ์ดเดียวกัน ส่วนตัวเอนจินยัง
ดูแค่ `[ai] enabled` ตัวเดียว — `vcut decide` ที่ตั้ง `mode = all/budget` แต่ตั้งใจใช้
คะแนน AI จึงยังทำงานเหมือนเดิม

### ขั้น 3 — "ให้ AI เลือก" กับคลังที่ใหญ่เกินไป

โหมดนี้ส่งคลังทั้งกองไปให้โมเดลเลือก ซึ่งพังเงียบ ๆ เมื่อคลังใหญ่ — **วัดจริงกับ
คลัง 398 ชิ้น: prompt 55 KB แล้วโมเดลใช้โควตา output 32,000 token หมดไปกับการคิด
จนยังไม่ทันตอบ JSON** (`stop_reason = max_tokens`) รอไป 11 นาทีแล้วได้ศูนย์

`[compose] ask_max` คัดตัวคะแนนดีที่สุดส่งไปก่อน ค่าตั้งต้น **150**

```
คลัง 398 ชิ้น (248 พูด + 150 วิว) 23.0 นาที   prompt 55 KB   ← ชนเพดาน
  ask_max = 250 → 250 ชิ้น (156 + 94) 18.8 นาที  prompt 40 KB
  ask_max = 150 → 150 ชิ้น ( 93 + 57) 14.7 นาที  prompt 27 KB   ← ค่าตั้งต้น
  ask_max = 100 → 100 ชิ้น ( 62 + 38) 11.5 นาที  prompt 21 KB
```

คงสัดส่วนพูด:วิว ของคลังเดิมไว้เป๊ะ (62:38 → 62:38) ไม่งั้นตัดแล้วเหลือแต่ช่วงพูด
ยาว ๆ ติดกัน · เรียงตามลำดับเล่าเรื่องก่อนส่ง (ไม่ใช่ลำดับใน `pool.json` ซึ่งจะ
เพี้ยนถ้าขั้น 1 ลากสลับที่ไว้) · **ชิ้นที่ถูกคัดออกยังอยู่ในคลังครบ** แค่ไม่ได้ถูก
เสนอให้ AI รอบนี้ — โหมดอื่นยังหยิบได้ตามปกติ

ถ้าแกะคำตอบไม่ได้ เอนจินขึ้นคำเตือนบอกสาเหตุ แล้ว **หยุดโดยไม่เขียนทับของเดิม**

```
⚠  AI ตอบกลับมาแต่ใช้ไม่ได้ — โมเดลใช้โควตา output หมดไปกับการคิด
   (ออกไป 32,000 token) จนยังไม่ทันตอบ JSON — stop_reason = max_tokens
⚠  ทางแก้: ลด [compose] ask_max ลง (ตอนนี้ 150 · ส่งไปจริง 150 ชิ้น) แล้วสั่งใหม่
✗  แกะ JSON จากคำตอบไม่ได้ — หยุดไว้ก่อน ไม่เขียนทับของเดิม
```

⚠️ ระวังกับดักที่เคยกลบสาเหตุนี้ไว้: `--output-format json` ทำให้ stdout **เป็น
ตัวซองเอง** การ `_extract_json(stdout)` เป็นทางสำรองจึงคืน "ซอง" มาเป็นคำตอบเสมอ
แล้วไปพังไกลอีกที่เป็น *"AI ไม่ได้เลือกชิ้นไหนเลย"* ตอนนี้ทางสำรองนั้นทำงานเฉพาะ
ตอนซองพังจนอ่านไม่ออกจริง ๆ เท่านั้น

### ขั้น 2 — ตัดช่วงเงียบให้ประโยคชนกัน (cut ชน)

`[talk]` ตัดตาม transcript ซึ่ง **whisper คืนช่วงมาหยาบมาก** — ฟุตเทจชุดนี้ 109 จาก
116 คลิปพูดได้ช่วงเดียวยาวทั้งคลิป ช่วงที่คนหยุดคิด หายใจ หรือเดินเงียบ ๆ จึงติดมา
ด้วยทั้งหมด ตัดตาม transcript อย่างเดียวจึงตัดอะไรออกไม่ได้เลย

เปิด `[jumpcut]` แล้วขั้น **หาช่วงเงียบ** จะฟังคลื่นเสียงจริงด้วย `ffmpeg silencedetect`
แล้ว `prepare` คว้านช่วงเงียบออกจากช่วงพูด ผลคือประโยคต่อประโยคติดกันไม่มีช่องว่างคั่น

```toml
[jumpcut]
enabled     = false
noise_db    = -32.0      # เบากว่านี้ถือว่าเงียบ (−45 เข้มงวด · −25 ตัดเยอะ)
min_silence = 0.45       # เงียบนานกว่านี้ถึงตัดออก (สั้นกว่านี้คือจังหวะหายใจ)
pad         = 0.10       # เผื่อข้างละเท่านี้ กันตัดโดนพยัญชนะต้น/ท้ายคำ
min_piece   = 0.60       # เศษที่สั้นกว่านี้ทิ้ง กันได้ชิ้นกระตุกครึ่งวินาที
```

วัดจริงกับฟุตเทจชุดนี้ — ฟัง 130 คลิปพูดใน **2.8 วินาที** เจอช่วงเงียบ 476 ช่วง
รวม 9.1 นาที (35% ของเวลาคลิปพูด) ตัดออกจริง 3.1 นาที ประโยคถูกซอยเพิ่ม 74 ชิ้น

```
IMG_7156   2.99– 5.65 │ 6.11– 9.40 │ 9.76–12.23 │ 12.54–15.20
           ต่อกันเป็นช็อตติดกัน 4 ช็อต — ช่องเงียบ 0.46 / 0.36 / 0.31 วิ หายไป
```

**ชิ้นที่มาจากประโยคเดียวกันถูกล็อกให้อยู่ติดกันเสมอ** (`jump` group) ไม่งั้นโหมด
`pattern` / `budget` ในขั้น 3 จะเอาช่วงวิวไปแทรกกลางประโยค ซึ่งไม่ใช่ cut ชน แต่คือ
ประโยคขาด — ทดสอบกับโหมด pattern แล้ว 22 กลุ่ม ไม่มีกลุ่มไหนถูกแทรกคั่น

สองค่าบน (`noise_db` `min_silence`) เปลี่ยนแล้วต้องกด **หาช่วงเงียบ** ใหม่ ·
สองค่าล่าง (`pad` `min_piece`) แค่กด **ตัดทีละคลิป** พอ — หน้าเว็บติดป้ายบอกไว้ที่ช่อง

### ขั้น 2 — คลังชิ้น และการดึงชิ้นที่ถูกกรองออกกลับมา

คลังแสดงแยกสองคอลัมน์ *วิดีโอพูด / วิดีโอวิว* กรองด้วย **ทั้งหมด / ใช้ได้ / ถูกกรองออก**
ชิ้นที่ตัวกรองคัดออกไม่ได้หายไปไหน — มันอยู่ในคลังพร้อมเหตุผลว่าโดนคัดเพราะอะไร
(`ภาพสั่น motion 22.4 ≥ 18` · `AI บอกว่าใช้ไม่ได้` · `ภาพมืด` · `ไม่มีเสียงพูด`)

กดที่ชิ้นเปิดกล่องที่**เล่นเฉพาะช่วงที่จะตัดจริง** — เล่นไฟล์ต้นฉบับแล้วกระโดดไปที่
`start` หยุดที่ `end` แล้วถอยกลับต้นช่วงให้ กดเล่นซ้ำได้เลยโดยไม่ต้องลากเอง
(ปิดสวิตช์ก็ดูทั้งคลิปได้) พร้อมแถบบอกว่าช่วงที่ตัดอยู่ตรงไหนของคลิป

ติ๊ก **ช่องเขียว** ที่ชิ้นไหน = ดึงกลับเข้าคลัง แล้วกด **บันทึก + เตรียมคลังใหม่**
ระบบเขียน `[prepare] keep` แล้วรัน `prepare` ให้ทันทีในคำขอเดียว (ไม่ถึงวินาที
เพราะ `prepare` อ่านจากไฟล์ที่ทำไว้แล้วล้วน ๆ ไม่แตะไฟล์วิดีโอ)

```toml
[prepare]
keep = ["IMG_7075", "IMG_7162"]   # ข้ามตัวกรองทุกตัวให้คลิปพวกนี้
```

**ทำไมต้องมี `keep` ในเมื่อคลายค่าตัวกรองก็ได้** — คลาย `drop_above_motion` จาก 18
เป็น 23 เพื่อเอาชิ้นเดียวกลับมา จะมีอีกสิบชิ้นที่ไม่ได้อยากได้หลุดเข้ามาด้วย
`keep` ยกเว้นให้ทีละชิ้นโดยไม่ขยับเกณฑ์ของชิ้นอื่นเลย

`[scan] exclude` (ขั้น 1) ชนะ `[prepare] keep` เสมอ — "ไม่เอาคลิปนี้" แรงกว่า
"ข้ามตัวกรองให้คลิปนี้"

**ไม่มีสวิตช์ "รันขั้นนี้" แล้ว** — เดิมมี `[run]` ในไฟล์โปรเจกต์ไว้ปิด/เปิดเป็นราย
Phase ตอนนี้ถอดออกทั้งชุด `./vcut run` และปุ่ม **ทำทุกขั้น** จึงวิ่งครบ 1→5 เสมอ
(ยังขึ้นกล่องยืนยันบอกก่อนเสมอว่าจะรันอะไรบ้าง ข้ามอะไรเพราะอะไร ใช้เวลาเท่าไร)

ที่ถอดออกเพราะทุกขั้นมีปุ่มสั่งตรงของตัวเองอยู่แล้ว สวิตช์ชุดนั้นจึงเหลือความหมาย
เดียวคือ *"ปุ่มทำทุกขั้นจะแวะไหนบ้าง"* ซึ่งคนอ่านสลับกับ *"ปิดฟีเจอร์ขั้นนั้น"* ตลอด
— และค่าตั้งต้นที่ปิดขั้น 4/5 ไว้ทำให้กดทำทุกขั้นแล้วไม่มีไฟล์ข้อความ/ไฟล์แต่งออกมา
โดยไม่มีอะไรบอกสาเหตุ  อยากรันไม่ครบ ใช้ `--from` หรือสั่งชื่อขั้นตรง ๆ แทน

**สวิตช์ที่ยังเหลือคือสวิตช์ "ปิดฟีเจอร์" ซึ่งคนละเรื่องกัน** — จุดนี้ผิดกันบ่อย:

```
--from compose    →  ไม่ทำขั้น 1–2 ใหม่ แต่ยังใช้ pool.json เดิมอยู่  (ประหยัดเวลา ผลเท่าเดิม)
ปิด ai.enabled    →  ไม่เอาความเห็น AI ไปใช้เลย                    (ผลกลับเป็นกฎล้วน)
```

`[ai] enabled` · `[jumpcut] enabled` · `[listen] enabled` เป็นสวิตช์ปิดฟีเจอร์ทั้งสามตัว
— ปิดแล้วขั้นนั้นถูกข้ามในแผนด้วย และหน้าเว็บบอกเหตุผลไว้ใต้ชื่อขั้น

### ปุ่มผลิตไฟล์ประจำขั้น

**ปุ่มบนการ์ดของขั้นไหน ทำแค่ขั้นนั้น** — คนละเรื่องกับ "ทำทุกขั้น"

| ปุ่ม | อยู่ที่ | รันอะไร | แตะ AI ไหม |
|---|---|---|---|
| **จัดใหม่** / **ให้ AI จัดให้** | ขั้น 3 | `compose` → เขียน `edl.json` | เฉพาะสาขา "ใช้ AI" |
| **สร้างไฟล์** | ขั้น 3 | `render` → `assemble` | ไม่เลย |
| **สร้างไฟล์แบบมีข้อความ** | ขั้น 4 | `render` → `caption` | ไม่เลย |
| **แต่งหนัง** | ขั้น 5 | `render` → `finish` | ไม่เลย |

ทั้งสามปุ่มล่างเติม `render` ให้เองก่อน จะได้ไม่ต้องเด้งกลับไปกดขั้นก่อนหน้าเวลา
มี segment ขาด — และเมื่อ segment ครบอยู่แล้ว `render` ใช้เวลา 0.1 วินาที

ปุ่มพวกนี้ทำตามที่เขียนไว้บนปุ่มเสมอ ไม่มีตัวกรองอื่นมาคัดทิ้งกลางทาง — เดิมปุ่มขั้น 4
กับขั้น 5 เดินผ่านตัวกรองเดียวกับปุ่ม "ทำทุกขั้น" ซึ่งคัด `caption`/`finish` ทิ้ง
เงียบ ๆ เพราะสวิตช์ `[run]` ตั้งต้นปิดสองขั้นนั้นไว้ — กดปุ่ม "สร้างไฟล์แบบมีข้อความ"
แล้วได้แค่ตัดชิ้นใหม่ ไม่มีไฟล์ข้อความออกมา และไม่มีอะไรบอกว่าทำไม  ตอนนี้สวิตช์ชุด
นั้นถูกถอดออกทั้งหมดแล้ว

ปุ่ม "สร้างไฟล์" ของขั้น 3 เคยสั่ง `vcut run` ทั้งไปป์ไลน์ กดคำเดียวแล้วมันถอดเสียงใหม่
ถาม AI ใหม่ เตรียมคลังใหม่ ก่อนจะถึงการต่อไฟล์ — เลือกโหมดกฎล้วนไว้แต่ได้ claude
รันครึ่งชั่วโมง ตอนนี้มันผลิตไฟล์จากไทม์ไลน์ที่เห็นอยู่เท่านั้น

### รีเซ็ต — และ "ค่าเก่า" ที่ดึงกลับมาได้

ปุ่ม **รีเซ็ตทั้งหมด** อยู่บนหัว ส่วน **รีเซ็ตขั้นนี้** อยู่บนหัวการ์ดของแต่ละขั้น
ทั้งสองปุ่มเปิดกล่องเดียวกัน ต่างกันแค่ขอบเขต และกล่องนั้นบอกครบสามอย่างก่อนกด:

```
ค่าที่ตั้งไว้        ลบค่าที่ทับไว้ 5 ตัว แล้วปล่อยให้ค่าตกมาจาก hiking-vlog ตามเดิม
                    ai.enabled        เปิด  →  ปิด
                    listen.import_dir …/v2_transcripts  →  (ว่าง)

ผลงานที่ทำไว้แล้ว    ☐ ความเห็น AI            376 KB   ลบแล้ว ต้องถาม AI ใหม่ — เสียโควตา
                    ☐ ชิ้นที่ตัดแล้ว        4.26 GB   ลบแล้ว ต้อง render ใหม่ทุกชิ้น (~40 นาที)

ค่าเก่าที่เก็บไว้     ก่อนรีเซ็ต ขั้น 2 · 03/08 01:15        [กู้เฉพาะขั้นนี้]
```

**รีเซ็ตค่า = ลบคีย์ที่โปรเจกต์ทับไว้ ไม่ใช่เขียนค่าตั้งต้นทับลงไป** ผลที่เห็นเหมือนกัน
แต่ความหมายต่างกันมาก — แบบแรกทำให้ค่าไหลลงมาจาก preset ตามปกติ วันที่ preset
เปลี่ยน โปรเจกต์นี้ก็ได้ตามไปด้วย ส่วนแบบหลังจะทำให้ไฟล์บวมขึ้นทุกครั้งที่กดรีเซ็ต

**ผลงานไม่ถูกแตะถ้าไม่ติ๊ก** และทุกรายการติดป้ายราคาของการทำใหม่ไว้ ตัวที่แพงจริง
(scan · ถอดเสียง · AI · segment cache) ขึ้นกรอบแดง

ก่อนแตะอะไรทั้งนั้น ระบบถ่ายสำเนา **ทั้งไฟล์โปรเจกต์** เก็บไว้ก่อนเสมอ — ข้อความดิบ
คอมเมนต์ครบทุกบรรทัด กู้คืนทีหลังจึงได้ของเดิมเป๊ะทุกตัวอักษร (กู้ทั้งไฟล์) หรือ
เฉพาะขั้นที่เลือก การกู้คืนเองก็ถูกถ่ายสำเนาไว้อีกชั้น กดผิดแล้วยังกลับมาได้

```bash
./vcut reset -c <project> --scope prepare        # ล้างค่าขั้น 2 (ถามยืนยันก่อน)
./vcut reset -c <project> --files ai,segments    # ลบผลงานด้วย
./vcut reset -c <project> --list                 # ดูค่าเก่าที่เก็บไว้
./vcut reset -c <project> --restore 20260803-091500
```

ประวัติเก็บที่ `.vcut-history/` ซึ่งอยู่ใน `.gitignore` — มันคือ *ที่ทำงานของเครื่องนี้*
ไม่ใช่ *ผลงานของโปรเจกต์* ถ้า commit ตามไปด้วย คนอื่น pull มาจะได้ประวัติการกดปุ่ม
ของเราติดไปเต็มไปหมด และ merge ชนกันทุกครั้งโดยไม่มีประโยชน์อะไรเลย

รีเซ็ต preset ใน `config/presets/` ไม่ได้ — มันเป็นสูตรที่ใช้ร่วมกันหลายโปรเจกต์และ
ถูก commit เข้า git ล้างทิ้งคือลบงานของคนอื่น ไม่ใช่ล้างงานของตัวเอง

### ราคาของการแก้ค่า

ทุกช่องติดป้ายราคาไว้ และแผงขวาคำนวณให้สด ๆ ทุกครั้งที่ขยับ:

```
เลื่อน talk.min_shot 3 → 6      →  render ใหม่ 37 ชิ้น · ประมาณ 8 นาที · ได้ 24:04
เลื่อน audio.allow_limit 12 → 4  →  render ใหม่ 75 ชิ้น · ประมาณ 14 นาที
เลื่อน broll.run_max 3 → 2       →  ไม่ต้อง render ใหม่ · ได้ 22:03
```

ตัวเลขนี้**แม่นจริง ไม่ใช่การเดา** และคำนวณเสร็จใน 0.02 วินาทีโดยไม่เรียก ffmpeg
เลยสักครั้ง — ทำได้เพราะชื่อไฟล์ segment คือ sha1 ของทุกพารามิเตอร์ที่มีผลต่อชิ้น
นั้น คำนวณ hash แล้วดูว่ามีไฟล์อยู่หรือยัง ก็รู้คำตอบทันที ส่วนอัตราวินาที/ชิ้น
มาจากที่ `render` วัดไว้จริงในรอบก่อน

| ระดับราคา | คีย์ | ผลของการแก้ |
|---|---|---|
| ไม่ต้องทำอะไรใหม่ | `render.workers` `project.out` | 0 |
| decide + ต่อไฟล์ใหม่ | `talk` `broll` `order` `select` `ai.apply` | ~35 วิ |
| ทำภาพตัวอย่างใหม่ | `thumbs` | ~2 นาที |
| ถาม AI ใหม่ | `ai.model` `ai.goal` `ai.tasks` | 5–20 นาที · เสียเงิน |
| ถอดเสียง/อ่านคลิปใหม่ | `listen` `scan` | 12–40 นาที |
| render ใหม่ทุกชิ้น | `video` `encode` `audio` | ~40 นาที |
| เขียนไฟล์ขั้น 4/5 ใหม่ | `caption.ffmpeg` `fx.out_suffix` + ทุกอย่างใน `captions.json` / `fx.json` | เข้ารหัสภาพทั้งเรื่อง 1 รอบ |

แถวสุดท้ายอยู่นอกตารางราคาที่หน้าเว็บคำนวณให้ — ของในสองไฟล์นั้นไม่ได้อยู่ในไฟล์
โปรเจกต์ จึงไม่มีป้ายราคาต่อช่อง แต่ราคาเท่ากันหมด: **แก้อะไรก็ตามในขั้น 4 หรือ 5
= เข้ารหัสภาพทั้งเรื่องใหม่หนึ่งรอบ** (ยกเว้นชิ้นที่ใส่เอฟเฟกต์ ซึ่งมี cache ของตัวเอง
ที่ `fxseg/` — แก้สีชิ้นเดียวไม่ได้ทำให้อีก 200 ชิ้นต้องตัดใหม่)

แท็บ **TOML ดิบ** แก้ได้ครบทั้ง 99 คีย์ ไม่จำกัดแค่ที่ฟอร์มมีให้ กดบันทึกแล้ว
ระบบลองโหลดดูก่อน ถ้า config ใช้ไม่ได้จะไม่เขียนทับไฟล์เดิม

การบันทึกจากฟอร์ม**แก้ทีละบรรทัดในไฟล์เดิม** ไม่ได้เขียนทับทั้งไฟล์ — คอมเมนต์
ที่อธิบายเหตุผลไว้และคีย์ที่ฟอร์มไม่รู้จักจึงอยู่ครบทุกตัว

### `/` — ดูและแก้ EDL

| ทำได้ | ทำไม่ได้ (ตั้งใจ) |
|---|---|
| ดูช็อตทั้งหมดพร้อมภาพ คำพูด คะแนน AI | ตัดต่อในเบราว์เซอร์ |
| กดฟัง/ดูชิ้นที่ render แล้วจริง ๆ | ยืด-หด ความยาวช็อต |
| เอาช็อตออก · เอากลับ · เอาออกทั้งบท | เปลี่ยนค่า config |
| ลากสลับลำดับ · ปุ่ม ↑↓ · คีย์ `j k x J K` | อัปโหลดฟุตเทจ |
| กด **สร้างไฟล์** แล้วดู log ไหลสด ๆ | |
| **ย้อนกลับ** ไปฉบับก่อนบันทึกล่าสุด | |

เบราว์เซอร์ทำได้แค่ *เอาออก* กับ *สลับลำดับ* — ซึ่งเป็นสองอย่างที่ **ไม่ต้อง render ใหม่**
ทุกอย่างที่เหลือยังเป็นงานของ config เหมือนเดิม วัดจริงกับฟุตเทจชุดนี้:

```
เอาช็อตออก 3 ชิ้น + สลับลำดับ → กดสร้างไฟล์ → final.mp4 ใหม่ใน 31 วินาที
(render 217 ชิ้น: cache ทั้งหมด · ใหม่ 0)
```

ทุกครั้งที่บันทึก ฉบับก่อนหน้าถูกสำรองไว้ที่ `.vcut/edl.prev.json` เสมอ

---

## ขั้น 4 · รวมเป็นหนังแบบมี Text

```bash
./vcut caption -c projects/phu-soi-dao.toml            # → final-text.mp4
./vcut caption -c projects/phu-soi-dao.toml -o ก.mp4   # ตั้งชื่อไฟล์เอง
```

**ขั้นนี้ไม่ตัดอะไรใหม่เลย** ใช้ segment ชุดเดียวกับขั้น 3 ทั้งหมด ต่างกันแค่ตอนต่อไฟล์:
ผ่านฟิลเตอร์ `ass` แล้วเข้ารหัสภาพใหม่หนึ่งรอบ ได้ไฟล์ตัวที่สองที่มีข้อความติดอยู่ในภาพ
ส่วน `final.mp4` ตัวเดิมไม่ถูกแตะ

### ต้องมี ffmpeg ที่เขียนตัวหนังสือได้

homebrew ย้ายฟิลเตอร์ตัวหนังสือออกจากสูตร `ffmpeg` ปกติไปไว้ที่ `ffmpeg-full` ซึ่งเป็น
keg-only (ติดตั้งแล้วไม่ทับตัวเดิม) เครื่องส่วนใหญ่จึงมี ffmpeg ที่ตัดต่อได้ครบแต่
**เขียนตัวหนังสือไม่ได้เลย และไม่มีอะไรบอกจนกว่าจะสั่งแล้วพัง**

```bash
brew install ffmpeg-full          # ไม่ทับ ffmpeg เดิม
```

vcut หาให้เอง (`ffmpeg-full` ก่อน แล้วค่อย `ffmpeg`) โดยยิง `-filters` ดูจริงว่ามี `ass`
ไหม ไม่ได้เดาจากเส้นทาง ระบุเองได้ที่ `[caption] ffmpeg = "…"` · หน้าเว็บขึ้นคำเตือน
พร้อมคำสั่งติดตั้งและ **ปิดปุ่ม** ไว้ตั้งแต่ก่อนกด ไม่ใช่ให้ไปเจอตอนรันไปแล้วครึ่งทาง

### ทำไมเป็น ASS ไม่ใช่ drawtext

| | `drawtext` | `ass` (libass) |
|---|---|---|
| ข้อความ 300 บรรทัด | ต่อฟิลเตอร์ 300 ตัว สายยาวเป็นกิโล | ฟิลเตอร์เดียว อ่านจากไฟล์ |
| สระ/วรรณยุกต์ไทย | **ซ้อนผิดตำแหน่ง** — freetype เปล่า ๆ ไม่ทำ shaping | ถูกทุกตัว (เรียก harfbuzz ให้) |
| แอนิเมชัน | ไม่มี | `\fad` `\t` `\move` `\fscx` … (ขั้น 5 ใช้) |
| วาดรูป | ไม่ได้ | โหมด `\p` วาดเวกเตอร์ได้ (ขั้น 5 ใช้) |

### ข้อความผูกกับ (คลิป, วินาทีในคลิป) ไม่ใช่วินาทีในหนัง

ไทม์ไลน์เป็นตัวเดียวกับขั้น 3 ซึ่งลากขอบ/ลบชิ้นได้ตลอด ถ้าจำเป็นวินาทีในหนังไว้ตรง ๆ
ข้อความทุกชิ้นหลังจุดที่แก้จะเลื่อนหลุดพร้อมกันหมดโดยไม่มีใครรู้ตัว

ผูกกับคลิปแล้วคำนวณวินาทีในหนังใหม่ทุกครั้งจากไทม์ไลน์ปัจจุบัน — **แก้ตรงไหนข้อความ
ก็ตามไปเอง** และชิ้นที่ถูกตัดทิ้ง ข้อความของมันก็หายไปด้วยอย่างที่ควรเป็น (หน้าเว็บ
ติดป้าย *กำพร้า* ให้ ไม่ได้ลบทิ้งเงียบ ๆ)

### `.vcut/captions.json`

```jsonc
{
  "version": 1,
  "style": { /* สไตล์กลางของทั้งเรื่อง — ตารางข้างล่าง */ },
  "auto": {
    "enabled": true,          // เอาซับจากบทพูดที่ถอดไว้ไหม
    "edits":  {"IMG_7156#3": "ข้อความที่แก้แล้ว"},   // whisper ได้ยินผิด
    "drop":   ["IMG_7156#7"],                        // บรรทัดที่ไม่เอา
    "styles": {"IMG_7156#3": {"color": "#FFD400"}}   // ทับสไตล์เฉพาะบรรทัด
  },
  "boxes": [                  // ข้อความที่เขียนเอง
    {"id": "b1", "name": "IMG_7162", "at": 2.5, "dur": 3.0,
     "text": "ยอดภูสอยดาว", "x": 0.5, "y": 0.18, "style": {"size": 72}}
  ]
}
```

`id` ของซับอัตโนมัติคือ `<ชื่อคลิป>#<ลำดับบรรทัดใน transcript>` — ผูกกับ *บทพูด*
ไม่ใช่กับตำแหน่งในหนัง จัดลำดับใหม่แล้วยังชี้ไปที่บรรทัดเดิมเสมอ

### สไตล์กลาง

| คีย์ | ค่าตั้งต้น | ความหมาย |
|---|---|---|
| `font` | `Sukhumvit Set` | ชื่อ family ตามที่ `fc-list` เห็น |
| `size` | 54 | **พิกเซลจริงของหนัง** — PlayRes ถูกตั้งเท่าขนาดภาพ เลขในหน้าเว็บจึงเป็นเลขเดียวกับที่ออกมาในไฟล์ |
| `color` / `outline` | `#FFFFFF` / `#000000` | สีตัวอักษร / สีขอบ |
| `border` / `shadow` | 3.0 / 0.0 | ความหนาขอบ / เงา |
| `bold` `italic` | false | |
| `align` | 2 | เลขแป้นตัวเลข — 1-3 ล่าง · 4-6 กลาง · 7-9 บน |
| `margin_v` / `margin_h` | 60 / 60 | ระยะจากขอบจอ (ใช้เมื่อไม่ได้ตรึงพิกัด) |
| `pos_x` / `pos_y` | `null` | ตรึงพิกัดเอง — **สัดส่วน 0–1 ไม่ใช่พิกเซล** |
| `spacing` | 0.0 | ระยะห่างตัวอักษร (`\fsp`) |
| `angle` | 0.0 | หมุนทวนเข็ม องศา (`\frz`) |

**ทำไมขนาดเป็นพิกเซลแต่ตำแหน่งเป็นสัดส่วน** — ขนาดตัวอักษรที่ผูกกับพิกเซลยังอ่านได้
เมื่อย่อความละเอียด (ตัวเล็กลงตามภาพ) แต่ *ตำแหน่ง* ที่ผูกกับพิกเซลจะหลุดออกนอกจอ
ทันทีที่ย่อจาก 4K เป็น 1080p

ทับได้เป็นราย ๆ ไม่เท่ากันตามชนิดของข้อความ:

```
ซับอัตโนมัติ  font size color outline border shadow bold italic spacing angle
              + align margin_v margin_h pos_x pos_y   ← ต้องติ๊ก "ย้ายเฉพาะบรรทัดนี้" ก่อน
กล่องเขียนเอง font size color outline border shadow bold italic spacing angle + align
              (ตำแหน่งเป็น x/y ของกล่องเองอยู่แล้ว)
```

ตำแหน่งรายบรรทัดเคยห้ามขาด เพราะซับที่วางไม่ตรงกันเป็นบรรทัด ๆ อ่านแล้วเหมือนหนังพัง
มากกว่าเหมือนตั้งใจ — ข้อกังวลนั้นยังจริงอยู่ แต่แก้ด้วยการทำให้เป็น **opt-in ราย
บรรทัด** ดีกว่าห้ามขาดแล้วคนที่ต้องหลบป้ายในภาพจริง ๆ ทำไม่ได้เลย

### รายละเอียดที่ทำให้ไม่พัง

| กลไก | ป้องกันอะไร |
|---|---|
| ชิ้นที่โผล่สั้นกว่า **0.20 วิ** ถูกข้าม | เศษซับที่ค้างตรงรอยตัด อ่านไม่ทันแต่กะพริบกวนตา |
| `_join` รวมชิ้นที่ห่างกัน < 0.05 วิ และเป็นบรรทัดเดียวกัน | ประโยคเดียวที่ถูก cut ชนผ่าครึ่ง แล้วสองครึ่งมาต่อกันพอดี — ถ้าปล่อยเป็นสองชิ้น libass จะดับแล้วจุดใหม่ เห็นเป็นตัวหนังสือกะพริบหนึ่งเฟรมทั้งที่คนพูดต่อเนื่อง |
| `_esc` แปลง `{` `}` `\` และขึ้นบรรทัดใหม่ | คนพิมพ์วงเล็บปีกกาทีเดียวข้อความหายทั้งบรรทัด (ASS อ่านเป็นคำสั่ง) |
| `\pos` มาพร้อม `\an` เสมอ | ไม่งั้นจุดที่พิกัดหมายถึงคือมุมของสไตล์กลาง ซึ่งอาจคนละมุมกับที่พรีวิววาดไว้ |
| อ่านความยาวชิ้นจาก `exact_dur` ไม่ใช่ `dur` ของ EDL | เศษเฟรมสะสมทีละชิ้นจนซับท้ายเรื่องเลื่อนหลายสิบมิลลิวินาที |
| ช่อง Dialogue ต้องครบ 9 ช่องก่อนข้อความ | ขาดคอมมาตัวเดียว แท็กจะถูกอ่านเป็นช่อง Effect แล้วโผล่เป็นตัวหนังสือให้คนดูเห็น |

### การ์ดขั้น 4 ในหน้าเว็บ

บนสุดของการ์ดมีสวิตช์ **"ใส่ซับจากบทพูดที่ถอดไว้"** (`auto.enabled`) ปิดแล้วในหนังจะมี
เฉพาะกล่องข้อความที่ใส่เอง — ซับมาจากขั้น 2 ที่ถอดไว้แล้ว **ไม่เรียก AI และไม่เสียโควตา**

ใต้ลงมาเป็นสามแท็บที่ตอบคนละคำถาม — รวมการ์ดเดียวแต่ไม่ปนกัน เพราะสามงานนี้ทำคนละเวลา:

| แท็บ | ตอบคำถาม | ทำอะไรได้ |
|---|---|---|
| **สไตล์กลาง** | หนังเรื่องนี้หน้าตายังไง | ฟอนต์ (ที่รองรับไทยขึ้นก่อน) · ขนาด · สี · ขอบ · เงา · ตำแหน่ง |
| **บรรทัดซับ** | whisper ได้ยินผิดตรงไหน | แก้ข้อความ · ปิดทีละบรรทัด · ทับสไตล์/ตำแหน่งเฉพาะบรรทัด |
| **กล่องข้อความ** | อยากเขียนอะไรเพิ่มเอง | เพิ่มกล่องตรงวินาทีที่ตัวเล่นค้างอยู่ · ลากวางบนภาพ |

พรีวิววาดทับตัวเล่นจริง และ **ลากตัวหนังสือบนภาพได้ตรง ๆ** — พิกัดที่ได้คือ `x`/`y`
สัดส่วนที่จะเขียนลงไฟล์เลย ไม่ใช่ค่าประมาณ เพราะทั้งหน้าเว็บและตัวเขียน ASS อ่าน
`caption.cues()` **ตัวเดียวกัน**

> ถ้าคำนวณคนละที่ เมื่อไรที่สองที่คิดไม่ตรงกันจะไม่มีใครรู้จนกว่าจะ render เสร็จ

ฟอนต์มาจาก `fc-list` จริงในเครื่อง แยกกลุ่ม *รองรับไทย* ขึ้นก่อน ที่เหลือต่อท้าย —
เลือกฟอนต์ที่ไม่มีในเครื่องไม่ได้ จึงไม่มีทางได้ไฟล์ที่ตัวหนังสือกลายเป็นสี่เหลี่ยม

### บทพูดเป็นไฟล์แยก (คนละเรื่องกับซับบนภาพ)

ถ้าอยากได้แค่ *ข้อความ* ไม่ต้องการให้ติดลงภาพ — `[listen] export` เขียนบทพูดต่อคลิป
ไว้ที่ `.vcut/text/` โดยไม่ต้องเข้ารหัสอะไรเลย

```toml
[listen]
export = "off"     # off | txt | srt | both
```

`txt` = ข้อความล้วนไว้อ่าน · `srt` = มีเวลา เอาไปโหลดใน player หรือ YouTube ได้เลย
(ดาวน์โหลดจากหน้าเว็บได้ที่ขั้น 2)

> **หมายเหตุ** กล่องรีเซ็ตของขั้น 4 ล้างได้แค่ *ค่าที่ตั้งไว้* (`[caption]`) ยังไม่มี
> รายการสำหรับลบ `captions.json` / `final-text.mp4` เหมือนที่ขั้น 5 มี — ลบด้วยมือได้
> ตามปกติ ทั้งสองไฟล์สร้างใหม่ได้จากหน้าเว็บ

---

## ขั้น 5 · แต่งหนัง

```bash
./vcut fx -c projects/phu-soi-dao.toml            # → final-fx.mp4
./vcut fx -c projects/phu-soi-dao.toml -o ข.mp4
```

```toml
[fx]
out_suffix = "-fx"       # final.mp4 → final-fx.mp4
```

`[fx]` มีคีย์เดียวโดยตั้งใจ — ที่เหลือทั้งหมดเป็นของที่ตั้ง *รายชิ้น* บนไทม์ไลน์
จึงอยู่ที่ `.vcut/fx.json` เหมือนที่ข้อความอยู่ที่ `.vcut/captions.json` ไม่ใช่ในไฟล์
โปรเจกต์ (ถ้าอยู่ในไฟล์ config จะกลายเป็นค่ากลางที่ตั้งต่างกันทีละชิ้นไม่ได้)

### ห้าชั้น หนึ่งพาส

```
1. ชิ้นที่แต่งแล้ว   สโลว์โม · ซูม · โทนสี · ปิดเสียง    fx.py       → .vcut/fxseg/
2. ต่อเป็นเส้นเดียว  concat
3. ข้อความ + รูปทรง  แอนิเมชัน · กล่องพื้นหลัง · ลูกศร   fxtext.py   → fx-captions.ass
4. ภาพซ้อน           PNG · สกรีนช็อต · วิดีโอโปร่งใส     overlay.py  → .vcut/assets/
5. เสียง             เพลงคลอ + หลบเสียงพูด + ความดังรวม  music.py
```

**ลำดับนี้ตายตัวและมีเหตุผลของมัน** — ข้อความต้องอยู่ *ใต้* ภาพซ้อน เพราะสติกเกอร์หรือ
การ์ดที่ตั้งใจวางทับข้อความต้องทับได้จริง ถ้าสลับกันจะไม่มีทางวางทับเลย · `loudnorm`
ต้องอยู่ *หลัง* ผสมเพลง ไม่งั้นวัดความดังจากเสียงพูดอย่างเดียวแล้วหนังจริงจะดังเกินที่ตั้งไว้

ชั้น 2–5 ยุบอยู่ใน **คำสั่ง ffmpeg เดียว** ทำทีละชั้นทับไฟล์ไปเรื่อย ๆ จะเสียคุณภาพ
สี่รอบและใช้เวลาสี่เท่า ทั้งที่ ffmpeg ต่อฟิลเตอร์ทุกตัวเข้าด้วยกันได้อยู่แล้ว

### `.vcut/fx.json`

```jsonc
{
  "version": 1,
  "clips": {                                    // เอฟเฟกต์รายชิ้นในไทม์ไลน์
    "IMG_7162@12.400+4.000": {"speed": 0.4, "zoom": 1.0, "grade": "warm", "mute": false}
  },
  "text": {
    "sub":   {"anim": "fade", "in": 0.18, "out": 0.14, "plate": false},  // ซับทั้งกอง
    "boxes": {"b1": {"anim": "pop", "in": 0.25, "out": 0.2, "plate": true}},
    "plate": {"color": "#000000", "alpha": 0.45, "pad": 14}              // ค่ากลาง
  },
  "shapes":   [{"id": "s1", "name": "IMG_7162", "at": 1.0, "dur": 2.0,
                "kind": "arrow", "x": 0.62, "y": 0.4, "size": 160,
                "thick": 0.28, "angle": 180, "color": "#FF3B30",
                "outline": "#000000", "border": 0, "anim": "pop"}],
  "overlays": [{"id": "o1", "name": "IMG_7162", "at": 0.5, "dur": 3.0,
                "file": "map.png", "x": 0.8, "y": 0.75, "width": 0.3,
                "opacity": 1.0, "angle": 0, "anim": "rise"}],
  "music":    {"file": "bgm.mp3", "gain_db": -18, "duck": true,
               "duck_db": 12, "duck_release": 400, "fade_in": 1, "fade_out": 2}
}
```

**กุญแจของชิ้นคือ `<คลิป>@<เริ่ม>+<ยาว>` ไม่ใช่ลำดับ** — ลำดับเปลี่ยนทุกครั้งที่ลาก
สลับชิ้นในไทม์ไลน์ ถ้าผูกกับลำดับ เอฟเฟกต์ที่ตั้งไว้กับช็อตหนึ่งจะกระโดดไปอยู่กับ
อีกช็อตทันทีที่มีคนสลับที่ โดยไม่มีอะไรบอก (หลักการเดียวกับที่ `render.py` ใช้เก็บผลวัดความดัง)

กุญแจที่ไม่ตรงกับชิ้นไหนเลย (ลบช็อตทิ้ง หรือลากขอบจนเปลี่ยนช่วง) เรียกว่า **กำพร้า**
— เอนจินเตือนแต่ไม่หยุด และหน้าเว็บบอกตั้งแต่ก่อนกดสร้างไฟล์ ไม่ใช่ให้ไปเจอเป็น
บรรทัดกลาง log

### ค่าที่ผิดถูกดัดตั้งแต่ตอนอ่านไฟล์

`fx.json` คนแก้ด้วยมือได้และต่อไป AI จะเขียนให้ — ค่าที่ผิดจึงต้องถูกดัด *ตรงที่อ่าน*
ไม่ใช่ปล่อยไปพังตอนประกอบสตริง ASS ซึ่งกว่าจะรู้ว่าพังเพราะอะไรต้องไปไล่อ่าน stderr

ดัดสามชั้น — **ชนิด · ช่วง · รายการที่มี** วัดจริงกับไฟล์ที่จงใจใส่ค่าพัง:

```
"speed": "0.5"      → 0.5          ชนิด: สตริงที่เป็นตัวเลขใช้ได้
"alpha": 2.5        → 1.0          ช่วง: ความทึบเกิน 1 ไม่มีความหมาย
"pad":   999        → 120
"size":  99999      → 2000
"anim":  "nope"     → "none"       รายการ: สะกดผิดจะเงียบไปเฉย ๆ ถ้าไม่ดัก
"kind":  "weird"    → "arrow"
"color": "not-a-colour" → "#000000"
```

ตรวจที่ `fx.py` ที่เดียว ทั้งตอนอ่านไฟล์และตอนบันทึกจากหน้าเว็บ — ไม่งั้นค่าที่ผ่าน
หน้าเว็บกับค่าที่คนพิมพ์เองจะถูกตรวจคนละมาตรฐาน แล้ววันหนึ่งจะมีค่าที่บันทึกผ่าน
หน้าเว็บได้แต่โหลดกลับมาไม่เหมือนเดิม

---

### ชั้น 1 · เอฟเฟกต์รายชิ้น — สโลว์โม · ซูม · โทนสี · ปิดเสียง

| ค่า | ช่วง | ความหมาย |
|---|---|---|
| `speed` | 0.1 – 8.0 | < 1 ช้าลง · > 1 เร็วขึ้น · **เปลี่ยนความยาวหนัง** |
| `zoom` | 1.0 – 4.0 | ดันเข้าโดยกรอบภาพไม่เปลี่ยน |
| `grade` | 5 ชุด | ตารางข้างล่าง |
| `mute` | — | ตัดเสียงเฉพาะชิ้นนี้ |

```
""      ไม่แตะสี
warm    อุ่น — แสงเย็น/ตอนเช้า           cool    เย็น — ป่าดิบ/สายฝน
punch   จัดจ้าน — คอนทราสต์+สีเข้ม        flat    จืด — ไว้คั่นหรือทำพื้นหลังข้อความ
bw      ขาวดำ
```

**เป็นชุดสำเร็จ ไม่ใช่ช่องปรับ R/G/B ทีละตัว** เพราะสิ่งที่คนตัดต่ออยากได้คือ "ให้
ช็อตนี้อุ่นขึ้น" ไม่ใช่ "เพิ่มแดง 0.06 ลดน้ำเงิน 0.04" — และชุดสำเร็จทำให้ทั้งเรื่อง
มีโทนเป็นชุดเดียวกัน ซึ่งเป็นเรื่องที่ตาจับได้ทันทีถ้าไม่มี

วัดจริงจากไฟล์ที่ออกมา:

| | ก่อน | หลัง |
|---|---|---|
| ความเคลื่อนไหว/เฟรม (`speed = 0.4`) | 3.85 | 1.65 → อัตรา **0.43** |
| `warm` (U / V) | 111.4 / 126.5 | 109.5 / 128.4 — น้ำเงินลด แดงขึ้น |
| `cool` (U / V) | 116.0 / 124.5 | 118.6 / 121.1 — น้ำเงินขึ้น แดงลด |
| `mute` | −20.3 dB | **−91.0 dB** (เงียบสนิท) |
| ซับที่อยู่หลังชิ้นสโลว์โม | 4.570 วิ | **9.076 วิ** — เลื่อนตามพอดี |

**ชิ้นที่ถูกแต่งทำจาก segment ของขั้น 3 ไม่ใช่ตัดจากไฟล์ต้นฉบับใหม่**

segment ของขั้น 3 ผ่านการหมุน · ใส่พื้นหลังเบลอ · แปลงช่วงสี · บังคับ CFR · ปรับความดัง
มาครบแล้ว ขั้น 5 จึงรับมันมาแล้วแต่งต่อ แทนที่จะตัดจากต้นฉบับพร้อมฟิลเตอร์ทั้งพวงอีกรอบ
— ซึ่งจะแปลว่าต้องก๊อปตรรกะการปรับมาตรฐานทั้งหมดมาไว้อีกชุด แล้ววันหนึ่งสองชุดจะปรับ
ไม่เหมือนกัน

ราคาที่จ่าย: **ชิ้นที่ใส่เอฟเฟกต์ถูกเข้ารหัสภาพเพิ่มหนึ่งรอบ** และการซูมทำจากภาพ 1080p
ที่ย่อมาแล้ว ไม่ใช่จาก 4K ต้นฉบับ — จ่ายเฉพาะชิ้นที่ตั้งใจแต่ง ส่วนชิ้นที่ไม่ได้แตะ
ใช้ไฟล์ของขั้น 3 ตรง ๆ ไม่เสียอะไรเลย

**มี cache เป็นของตัวเองที่ `.vcut/fxseg/`** หลักการเดียวกับขั้น 3 เป๊ะ — ชื่อไฟล์คือ
sha1 ของฟิลเตอร์ + จำนวนเฟรม + **ลายเซ็นของ segment ต้นทาง** (ต้องมีตัวหลังด้วย เพราะ
ขั้น 3 สร้างไฟล์ใหม่ได้ตลอดถ้ามีคนแก้ค่าฝั่งนั้น ไม่งั้นจะได้ cache ของภาพเก่า)

`vcut gc` เก็บกวาด `fxseg/` ให้ด้วย และถ้าคำนวณรายการที่ต้องเก็บไม่ได้ มัน **ข้ามการ
เก็บกวาดไปเลย** ไม่ใช่ถือว่า "ไม่ต้องเก็บอะไร" แล้วลบทั้งโฟลเดอร์

รายละเอียดเล็ก ๆ สองข้อ:

* ความยาวหลังใส่ speed ต้อง **ลงกริดเฟรม** เหมือนขั้น 3 — หารด้วยความเร็วเฉย ๆ จะได้
  เศษเฟรมที่สะสมทีละชิ้นจนซับท้ายเรื่องเลื่อน
* เสียงใช้ `atempo` ไม่ใช่ `asetrate` — `asetrate` เปลี่ยน *ระดับเสียง* ไปด้วย สโลว์โม
  0.4 เท่าจะทำให้เสียงคนกลายเป็นเสียงผี · `atempo` รับได้ทีละ 0.5–2.0 เท่านั้น
  นอกช่วงถูกซอยเป็นหลายทอดให้เอง (0.4 → `atempo=0.5,atempo=0.8`)

### `.vcut/fx-render.json` — "หลังใส่เอฟเฟกต์แล้วหนังมีชิ้นอะไร ยาวเท่าไร"

เอฟเฟกต์อย่างสโลว์โมเปลี่ยน *ความยาว* ของชิ้น ทุกอย่างที่อยู่หลังจากนั้นจึงเลื่อนตามหมด
รวมทั้งซับที่ต้องขึ้นให้ตรงปาก ถ้าขั้น 5 อ่านความยาวจาก `render.json` ตรง ๆ ตัวเลขที่ได้
จะเป็นของ *ก่อน* ใส่เอฟเฟกต์เสมอ แล้วซับตั้งแต่ชิ้นนั้นไปจะเลื่อนทั้งเรื่องโดยไม่มีใครรู้
จนกว่าจะนั่งดูไฟล์จนจบ

ไฟล์นี้ **คำนวณใหม่ทุกครั้ง ไม่เคยอ่านของเก่า** เพราะเป็น *ผลลัพธ์* ของสองไฟล์ที่แก้ได้
ตลอด ไม่ใช่สถานะที่ต้องจำ · ไม่มีสโลว์โมสักชิ้น มันก็เท่ากับ `render.json` ทุกช่อง

---

### ชั้น 3 · ข้อความเคลื่อนไหว + กล่องพื้นหลัง

ขั้น 4 เป็นเจ้าของ *เนื้อข้อความกับหน้าตานิ่ง ๆ* · ขั้น 5 เป็นเจ้าของ *การเคลื่อนไหว*
ผูกกันด้วย `id` ของกล่อง แบ่งแบบนี้แล้ว `captions.json` ไม่ต้องถูกแตะเลย

| `anim` | ทำอะไร |
|---|---|
| `none` | ขึ้นทันที |
| `fade` | ค่อย ๆ จาง เข้า–ออก (`\fad`) |
| `pop` | เด้งเข้ามา ย่อ→ใหญ่เกิน→พอดี (`\fscx`+`\t`) |
| `rise` | ไถลขึ้นจากข้างล่าง (`\move`) |
| `slide` | ไถลเข้าจากข้าง (`\move`) |

`in` / `out` = วินาทีที่ใช้เข้า/ออก (0–1.5) · `plate` = วาดกล่องทึบไว้ข้างหลังไหม

**ซับทั้งกองตั้งค่าตัวเดียว** ส่วนกล่องที่เขียนเองตั้งแยกทีละกล่องได้ — ซับที่เคลื่อนไหว
ไม่เหมือนกันทีละบรรทัดอ่านแล้วเหมือนหนังพัง ไม่ใช่เหมือนตั้งใจ

⚠️ **`rise` / `slide` ต้องรู้พิกัดที่แน่นอน** ถึงจะสั่ง `\move` ได้ ซับที่วางด้วย
align+margin ปล่อยให้ libass จัดตำแหน่งเอง เราจึงไม่รู้ว่ามันจะไปอยู่ตรงไหนจริง ๆ
(ขึ้นกับจำนวนบรรทัดและความสูงบรรทัดของฟอนต์) สองตัวนี้จึง **ตกกลับไปเป็น `fade` ให้แทน**
ดีกว่าเดาพิกัดผิดแล้วข้อความไถลไปโผล่คนละที่กับที่พรีวิววาดไว้ — ตรึงพิกัดเองในขั้น 4
แล้วมันจะทำงานตามที่สั่ง

**เวลาแอนิเมชันถูกหดให้พอดีกับความยาวชิ้นเสมอ** (ไม่เกิน 90% ของชิ้น) — ซับจากบทพูดมี
ชิ้นสั้นระดับ 0.3 วินาทีเป็นเรื่องปกติ ถ้าปล่อยไปตามที่สั่ง มันจะยังไม่ทันขึ้นเต็มตัว
ก็เริ่มจางออกแล้ว เห็นเป็นแค่ตัวหนังสือแวบ ๆ ทั้งที่ตั้งค่าไว้ดูดี

กล่องพื้นหลังใช้ `BorderStyle=3` ซึ่ง **เปลี่ยนความหมายของสองช่อง**: `OutlineColour`
กลายเป็น *สีกล่อง* และ `Outline` กลายเป็น *ระยะขอบรอบตัวหนังสือ* — แท็กที่ขั้น 4 ส่งมา
(ซึ่งยังหมายถึงขอบตัวอักษร) จึงถูกกรองทิ้งบนบรรทัดที่เปิดกล่องไว้ ไม่งั้นได้กล่องสีมั่ว
ขนาดมั่ว · สไตล์กล่องถูกเขียนลงไฟล์ **เฉพาะตอนมีคนใช้จริง** ไฟล์ที่ไม่มีกล่องจะเหมือน
ของขั้น 4 ทุกไบต์ ซึ่งทำให้เทียบสองขั้นตอนหาสาเหตุง่ายกว่ามาก

### ชั้น 3 (ต่อ) · รูปทรงเวกเตอร์

libass วาดเส้นเองได้ด้วยโหมด `\p` — ลูกศร/แถบ/จุด จึงไม่ต้องพึ่งไฟล์ภาพจากข้างนอก
และไม่ต้องเพิ่มฟิลเตอร์อะไรในสายเลย

```
kind    arrow | bar | dot          size    พิกเซลของหนังจริง (4–2000)
x / y   สัดส่วน 0–1                 thick   ความหนาเทียบกับ size (0.03–0.9)
angle   หมุนทวนเข็ม องศา            color / outline / border
anim    ใช้ชุดเดียวกับข้อความ
```

รูปทรงถูกวาดโดยยึด **(0,0) เป็นกึ่งกลางของรูป** และวางด้วย `\an5` พิกัด `x`/`y` ที่ลากใน
หน้าเว็บจึงหมายถึงกลางรูปพอดี ตรงกับที่ตาเห็น ไม่ใช่มุมบนซ้ายที่มองไม่เห็น · วงกลมทำจาก
เบซิเยร์ 4 ท่อน (ค่าคงที่ 0.5523)

เส้นทางถูกคำนวณใน Python **ที่เดียว** แล้วส่งให้หน้าเว็บไปวาดต่อ (แปลงเป็น SVG ได้ตรง ๆ
เพราะทั้งสองใช้พิกัดสัมบูรณ์เหมือนกัน) — ถ้าให้เบราว์เซอร์คำนวณรูปเอง วันหนึ่งลูกศรใน
พรีวิวจะคนละทรงกับในไฟล์ แล้วคนจะเชื่อพรีวิวจนกว่าจะ render เสร็จ

รูปทรงอยู่ **Layer 2** เหนือข้อความเสมอ — มันเป็นของที่ตั้งใจชี้ไปที่อะไรสักอย่าง

---

### ชั้น 4 · ภาพซ้อน

รูปทรงเวกเตอร์วาดสกรีนช็อตแผนที่หรือการ์ดคอมเมนต์ไม่ได้ — ของพวกนั้นเป็น *ไฟล์ภาพจาก
ข้างนอก* ชั้นนี้จึงเป็นทางเดียวที่ของแบบนั้นจะเข้ามาอยู่ในหนังได้ และเป็นทางที่คัตเอาต์คน
(ตัดพื้นหลังด้วยเครื่องมืออื่นแล้ว export เป็นวิดีโอโปร่งใส) เข้ามาได้ด้วย

```
รับ    .png .jpg .jpeg .webp        .mov .webm .mp4 .m4v
อยู่ที่ .vcut/assets/                ใหญ่สุด 40 MB ต่อไฟล์
```

**`fx.json` เก็บแค่ชื่อไฟล์ ไม่ใช่เส้นทางเต็ม** โปรเจกต์จึงยกไปเครื่องอื่นได้ทั้งก้อน
และหน้าเว็บอ่านไฟล์นอกโปรเจกต์ไม่ได้แม้จะพยายาม

| ค่า | ความหมาย |
|---|---|
| `width` | **สัดส่วนของความกว้างจอ** (0.4 = กว้าง 40% ของจอ) |
| `x` / `y` | ตำแหน่งกึ่งกลางภาพ สัดส่วน 0–1 |
| `opacity` `angle` | ความทึบ · หมุน (พื้นที่ที่เกิดจากการหมุนเป็นสีใส ไม่ใช่ดำ) |
| `anim` | `none` `fade` `rise` `slide` — **ไม่มี `pop`** |

`width` เป็นสัดส่วนเพราะสกรีนช็อตจากมือถือกว้าง 1170 กับสติกเกอร์กว้าง 240 ต้องสั่งด้วย
เลขที่หมายถึงสิ่งเดียวกัน ไม่งั้นต้องมานั่งคิดเลขทุกครั้งที่เปลี่ยนไฟล์ — และค่าที่ตั้งไว้
ยังถูกอยู่เมื่อเปลี่ยนความละเอียดหนัง

**ไม่มี `pop` ให้เลือกเพราะมันจะช้า** — `overlay` รับสูตรที่มี `t` ได้ จึงขยับตำแหน่ง
ตามเวลาได้ฟรี ๆ แต่ *ย่อ-ขยายตามเวลา* ต้องปรับขนาดภาพใหม่ทุกเฟรม ดีกว่ามีตัวเลือกที่
กดแล้วช้าลงสามเท่าโดยไม่บอก

**ภาพนิ่งกับวิดีโอเข้าคนละทาง:**

```
ภาพนิ่ง  -loop 1 -t <ยาวทั้งเรื่อง>    สตรีมเดินคู่ไปกับหนังตั้งแต่วินาทีที่ 0
                                      เวลาในสตรีม = เวลาในหนัง สูตรเฟดจึงเขียนตรง ๆ ได้
วิดีโอ   -itsoffset <วินาทีที่จะโผล่>   เลื่อนเวลาที่ระดับ container ให้เฟรมแรกไปตกตรง
                                      วินาทีที่ต้องการ — คลิปจึงเล่นตั้งแต่ต้นเรื่องของมัน
                                      เองตอนโผล่ ไม่ใช่เล่นค้างมาแล้วครึ่งเรื่อง
```

ทั้งสองทางจบที่ `enable='between(t,a,b)'` เหมือนกัน — นอกช่วงนั้น overlay **ไม่ทำงานเลย**
ไม่ใช่วาดภาพใสทับ (ต่างกันที่ความเร็ว: อย่างหลังเสียเวลาผสมทุกเฟรมทั้งเรื่อง)

**อัปโหลดผ่านหน้าเว็บ** ส่งเป็น base64 บน JSON ไม่ใช่ multipart เพราะ `http.server` ของ
stdlib ไม่มีตัวแกะ multipart ให้ และเขียนเองเป็นโค้ดที่พลาดแล้วเป็นช่องโหว่ได้ง่าย
ราคาที่จ่ายคือไฟล์โตขึ้น 33% ตอนส่ง ซึ่งไม่มีผลอะไรเลยกับการอัปโหลดในเครื่องตัวเอง

ทดสอบความปลอดภัยแล้วผ่านทุกข้อ:

```
ชื่อซ้ำ                      → เติมเลขต่อท้าย (ของเดิมอาจถูกใช้อยู่ในไทม์ไลน์)
../../escape.png            → เหลือแค่ escape.png
นามสกุลที่ไม่รองรับ           → ปฏิเสธพร้อมบอกว่ารับอะไรบ้าง
base64 พัง / ไฟล์ว่าง        → ปฏิเสธ
GET /asset/..%2f..%2fetc%2fpasswd → 404
```

ไฟล์ที่หายไปจากโฟลเดอร์ `assets` **ไม่ทำให้สร้างไฟล์ไม่ได้** — เตือนแล้วข้ามชั้นนั้นไป

---

### ชั้น 5 · เพลงประกอบ + หลบเสียงพูดอัตโนมัติ

```
file          ชื่อไฟล์ใน .vcut/assets/  (.mp3 .m4a .aac .wav .flac .ogg .opus)
gain_db       ดังแค่ไหนเทียบกับต้นฉบับ   ตั้งต้น −18
duck          หลบเสียงพูดอัตโนมัติไหม
duck_db       หลบลงประมาณกี่ dB ตอนมีเสียง   ตั้งต้น 12
duck_release  ms กว่าจะกลับมาดังเท่าเดิมหลังเงียบ   ตั้งต้น 400
fade_in / fade_out   วินาที
```

เพลงถูก `-stream_loop -1` แล้วตัดให้ยาวเท่าหนังพอดี — ไฟล์สั้นกว่าหนังก็วนให้เอง

**หลบเสียงพูดได้โดยไม่ต้องรู้ว่าใครพูดตรงไหน**

วิธีที่คนมักทำคือไล่ดูซับแล้วเขียน `volume` แบบมี `enable` เป็นช่วง ๆ ซึ่งต้องอาศัย
transcript ที่แม่นและ **พังทันทีที่มีคนแก้ไทม์ไลน์** · `sidechaincompress` ฟัง *แทร็กเสียง
จริง* แล้วลดอีกแทร็กลงตามความดังที่ได้ยินทุกมิลลิวินาที — ไม่ต้องรู้จักคำพูด ไม่ต้องพึ่ง
whisper และตามไทม์ไลน์ที่แก้แล้วเองเสมอ

เสียงหัวเราะ เสียงน้ำตก เสียงลม ก็ทำให้เพลงหลบเหมือนกัน ซึ่งเป็นสิ่งที่ต้องการจริง ๆ
อยู่แล้ว: เพลงควรหลบ *"ตอนมีอะไรน่าฟัง"* ไม่ใช่ *"ตอนมีคนพูด"*

⚠️ **`ratio` ไม่ใช่ปุ่มที่คุมความลึกของการหลบ — `threshold` ต่างหาก**

ปุ่มนี้เคยผูกกับ `ratio` ตรง ๆ แล้ววัดจริงพบว่ามันแทบไม่ขยับ:

```
ratio      4 → หลบ  7.0 dB  ·  12 → 8.5  ·  20 → 8.8      ← ปุ่มที่หมุนแล้วไม่มีอะไรเกิด
threshold −46 → หลบ 19.6 dB · −38 → 12.6 · −30 → 5.3 · −23 → 0.4   ← ตัวจริง
```

ปุ่มที่หมุนแล้วไม่มีอะไรเกิดขึ้นแย่กว่าไม่มีปุ่ม จึงตรึง `ratio` ไว้แล้วเปิดให้สั่งเป็น
**"หลบลงกี่ dB"** โดยแปลงเป็น threshold ให้ · สอบเทียบกับเสียงพูดจริงในหนังแล้ว:

| ตั้งไว้ | 4 | 8 | 12 | 16 | 20 |
|---|---|---|---|---|---|
| **หลบจริง** | 5.9 | 8.7 | 12.2 | 15.9 | 19.6 |

ช่วง 8–20 คลาดไม่ถึง 1 dB · **ต่ำกว่า 4 ไม่เปิดให้ตั้ง** เพราะจะเริ่มโกหก (ตั้ง 0 ได้หลบ
จริง ~5 dB) และ "ไม่อยากให้หลบ" มีสวิตช์ปิดอยู่แล้ว

เป็นค่าประมาณ เพราะความลึกจริงขึ้นกับว่าเสียงในหนังดังแค่ไหนด้วย — คนพูดเบา เพลงก็หลบ
น้อยลงตามจริง ซึ่งเป็นพฤติกรรมที่ถูกอยู่แล้ว

**สายเสียงทั้งหมดถูกประกอบใหม่ที่นี่** พอมีเพลง เสียงหนังจะไม่ใช่การก๊อปแทร็กเดียวออกมา
อีกต่อไป จึงต้องย้าย `loudnorm` (`[audio] master_lufs`) เข้ามาอยู่ในสายเดียวกัน — ทำทีหลัง
แยกต่างหากไม่ได้ เพราะความดังรวมต้องวัดจากเสียงที่ *ผสมเพลงแล้ว*

⚠️ `amix` ตั้งต้นเป็น `normalize=1` ซึ่ง **หารความดังด้วยจำนวน input** — ใส่เพลงเข้าไป
แล้วเสียงพูดจะเบาลงครึ่งหนึ่งทันทีโดยไม่มีอะไรบอก vcut ตั้ง `normalize=0` ให้เสมอ

---

### เพลงคลอสังเคราะห์ 53 ลูป — ไม่มีลิขสิทธิ์ ไม่ต้องต่อเน็ต

`vcut-ui/scripts/gen_bgm.py` ปั้นเพลงคลอด้วย numpy ล้วน (ยืมชุดฟิลเตอร์จาก `gen_sfx.py`
ตัวเดียวกัน) ลง `vcut-ui/public/bgm/` ลูปละ 15–30 วินาที เข้าคลังของโปรเจกต์ให้เองตอนใช้
ครั้งแรก แล้วเป็นแทร็กเพลงธรรมดาในสายตาเอนจิน (วนซ้ำ + หลบเสียงพูดตามค่าตั้งต้น)

**11 หมวด แบ่งเป็นสองชุดที่จัดคนละแบบ** — 6 หมวดแรกเรียงตาม *อารมณ์เพลง* ใช้กับหนัง
ทุกแนว: **เดินทาง · ชิล/โลไฟ · อบอุ่น/ซึ้ง · สนุก/มีพลัง · ลุ้นระทึก · คอรัส**
· 5 หมวดหลังหมวดละ 6 เพลง เรียงตาม *ช่วงของเรื่อง* ในทริปเดินป่า/ขึ้นเขา:
**ออกเดินทาง · เดินป่า/ลุยทาง · ขึ้นถึงยอด · แคมป์/กลางคืน · ขากลับ/ปิดเรื่อง**
เพราะคนตัด vlog แนวนี้เลือกเพลงจาก "ตอนนี้อยู่ช่วงไหนของทริป" ก่อนเสมอ แล้วค่อยดูว่า
จะเอาแบบมีพลังหรือแบบเงียบ — ทุกหมวดในชุดหลังมีเพลง **ไม่มีกลอง** อย่างน้อยหนึ่งเพลง
ไว้รองบทพูดยาว ๆ (เพลงมีกลองวางใต้คนพูดแล้วแย่งจังหวะกับคำเสมอ ต่อให้หรี่ไป 18 dB)

ชุดเดินป่ามีบางเพลงที่ใส่ **พื้นเสียงกลางแจ้งเป็นชั้นเบา ๆ** ใต้ดนตรี (ลม นก แมลง
กลางคืน ลำธาร กองไฟ ฝนบนผ้าเต็นท์ · ระดับ 3–6% เท่านั้น เพราะมันเป็น *กลิ่น* ของฉาก
ไม่ใช่เสียงบรรยากาศจริง ซึ่งเสียงในคลิปมีอยู่แล้วและดังกว่าเสมอ) — พื้นเสียงพวกนี้ถูก
ปั้นให้ยาวเท่าลูปพอดีและวนได้ด้วยตัวเอง ไม่ปล่อยให้กลไกพับหางจัดการเหมือนโน้ตเครื่อง
ดนตรี เพราะมันดังตลอดลูปไม่มีเว้น พับหางแล้วต้นลูปจะมีพื้นเสียงซ้อนสองชั้นทุกรอบ
= ดังขึ้น ~3 dB เป็นห้วง ๆ ซึ่งกับเสียงราบเรียบแบบนี้หูจับได้ทันที

```
python3 vcut-ui/scripts/gen_bgm.py            # ปั้นใหม่ทั้ง 53 ลูป (~2 นาที)
python3 vcut-ui/scripts/gen_bgm.py lofi-cafe  # ปั้นเฉพาะเพลงเดียว
```

**สองเรื่องที่ต่างจากเสียงเอฟเฟกต์ และเป็นเหตุผลที่ต้องมีสคริปต์แยก**

*ลูปต้องต่อหัว-ท้ายได้จริง* — ไขว้เฟดหัวกับท้ายแบบเสียงบรรยากาศใช้ไม่ได้ เพราะมันทำให้
ความยาวลูปสั้นลงเท่าช่วงที่ไขว้ จังหวะตรงรอยต่อจึงขาดไปเศษหนึ่ง แล้วทุกครั้งที่วนคนฟัง
จะได้ยินว่าสะดุด · ที่นี่เล่นเกินไปหนึ่งห้องแล้ว **พับหางกลับไปบวกที่หัวลูป** หางเสียง
สะท้อนของห้องสุดท้ายจึงไปโผล่ที่หัวของรอบใหม่พอดี เหมือนวงเล่นต่อเนื่องไม่มีหยุด

*ทั้งชุดต้องดังเท่ากันด้วย LUFS ไม่ใช่พีค* — `gen_sfx.py` นอร์มด้วยพีคซึ่งถูกสำหรับเสียง
เอฟเฟกต์ (ของที่มี "จุดเดียวที่ดัง") แต่เพลงที่แน่นทั้งลูปกับเพลงที่มีแต่แพดเบา ๆ พีค
เท่ากันได้ทั้งที่ดังต่างกันเป็นสิบ dB — วัดจริงตอนนอร์มด้วยพีค ห่างกัน 11 LUFS ระหว่าง
`choir-hum` กับ `up-energy` ตอนนี้ทุกลูปอยู่ที่ −16 LUFS (±1.4) วัดด้วย `loudnorm` แล้ว
คูณเกนเองใน numpy — ไม่ปล่อยให้ฟิลเตอร์ปรับ เพราะมันรีแซมเปิลภายในแล้วเติม/ตัดแซมเปิล
หัวท้าย ซึ่งพังรอยต่อของลูปพอดี

---

### การ์ดขั้น 5 ในหน้าเว็บ

ห้าแท็บ ตรงกับห้าชั้น — ตัวเลขบนแท็บบอกว่ามีของอยู่กี่ชิ้น

| แท็บ | ทำอะไร |
|---|---|
| **คลิป** | ทุกชิ้นในไทม์ไลน์ ตั้งความเร็ว/ซูม/โทนสี/ปิดเสียงทีละชิ้น |
| **ข้อความ** | แอนิเมชันของซับทั้งกอง · ของกล่องทีละกล่อง · หน้าตากล่องพื้นหลัง |
| **รูปทรง** | เพิ่ม/ลาก/หมุนลูกศร แถบ จุด |
| **ภาพซ้อน** | อัปโหลด · ลากวาง · ตั้งเวลาโผล่ |
| **เพลง** | เลือกไฟล์ · ความดัง · ความลึกของการหลบ · เฟด |

พรีวิววาดจาก **ตัวเลขชุดเดียวกับที่จะกลายเป็นไฟล์จริง** — ข้อความ รูปทรง ภาพซ้อน กล่อง
พื้นหลัง คำนวณใน Python ทั้งหมดแล้วส่งมาให้เบราว์เซอร์วาด ไม่ใช่ให้เบราว์เซอร์คิดเอง

ปุ่ม **แต่งหนัง** บันทึกสิ่งที่แก้ค้างไว้ให้เองก่อนรัน และปฏิเสธถ้าไทม์ไลน์ยังไม่ได้บันทึก
— ไฟล์ผลิตจากไทม์ไลน์ *ฉบับที่บันทึกไว้* ส่วนตัวเล่นเดินตามฉบับที่กำลังแก้ ปล่อยผ่าน
แล้วไฟล์จะไม่ตรงกับที่เพิ่งเห็นบนจอ

---

### ไม่ตั้งอะไรเลย → ได้ไฟล์เหมือนขั้น 4 เป๊ะ

นี่เป็นข้อบังคับที่ตรวจได้ ไม่ใช่ความตั้งใจ — วัดจริงหลังทำครบทุกเฟส:

```
fx-captions.ass  เทียบกับ  captions.ass    เหมือนกันทุกไบต์
ภาพ                                        1408/1408 เฟรม frame-md5 ตรงทุกเฟรม
เสียง                                      sample-md5 ตรง
```

ทำได้เพราะเมื่อไม่มีภาพซ้อนและไม่มีเพลง `finish.py` **จงใจคงเส้น `-vf` / `-af` เดิมไว้**
แทนที่จะใช้ `filter_complex` ที่ให้ผลเท่ากัน — เพื่อให้ข้อความข้างบนเป็นสิ่งที่พิสูจน์ได้
ด้วยการเทียบไฟล์ ไม่ใช่สิ่งที่ต้องเชื่อ

### รีเซ็ตทีละอย่าง

กล่องรีเซ็ตของขั้น 5 แยกเป็น 5 รายการ พร้อมป้ายราคาของการทำใหม่:

| รายการ | ลบอะไร | ทำใหม่แพงไหม |
|---|---|---|
| ชั้นเอฟเฟกต์ที่ตั้งไว้ | `fx.json` | ⚠️ ตั้งใหม่เองทั้งหมด |
| รายการชิ้นของขั้น 5 | `fx-render.json` · `fx-captions.ass` | สร้างใหม่เองทันที |
| ชิ้นที่แต่งแล้ว | `fxseg/` | ต้องเข้ารหัสชิ้นที่แต่งไว้ใหม่ |
| ไฟล์ภาพซ้อน/เพลงที่อัปโหลด | `assets/` | ⚠️ ต้องอัปโหลดใหม่ |
| ไฟล์หนังที่แต่งแล้ว | `final-fx.mp4` | กดแต่งหนังใหม่ |

---

## cache

```
.vcut/
├─ manifest.json      คุณสมบัติคลิป — cache ด้วย (ขนาด + mtime)
├─ transcript.json    คำพูด — cache รายคลิป
├─ loudness.json      ผลวัดเสียงรายช่วง
├─ ai.json            ★ ความเห็น AI (commit ได้)
├─ ai/                prompt ที่ส่งไป + คำตอบดิบ
├─ silence.json       ช่วงที่ไม่มีคนพูดในแต่ละคลิป (ใช้ตัดชน)
├─ pool.json          คลังชิ้นจากขั้น 2 (รวมชิ้นที่ถูกกรองออก + เหตุผล)
├─ compose.json       ชิ้นที่ AI เลือกให้ ตอนใช้ compose --ask
├─ review.json        ข้อเสนอของ AI ที่ดูหนังตัดแล้ว
├─ edl.json           ★ สัญญากลาง
├─ edl.prev.json      ฉบับก่อนหน้าที่ viewer สำรองไว้ให้
├─ render.json        map ชิ้น → ไฟล์ segment
├─ whisper/           ผลดิบจาก whisper.cpp
├─ text/              บทพูดเป็น .txt/.srt ต่อคลิป ([listen] export)
├─ thumbs/            ภาพตัวอย่าง + sheets/
├─ segments/          <content-hash>.mp4
│
├─ captions.json      ★ ขั้น 4 · ข้อความ (commit ได้)
├─ captions.ass       ไฟล์ซับที่ส่งให้ ffmpeg รอบล่าสุด
│
├─ fx.json            ★ ขั้น 5 · ชั้นเอฟเฟกต์ (commit ได้)
├─ fx-render.json     รายการชิ้นหลังใส่เอฟเฟกต์ — คำนวณใหม่ทุกครั้ง
├─ fx-captions.ass    ไฟล์ซับของขั้น 5 (มีแท็กแอนิเมชัน + รูปทรง)
├─ assets/            ⚠️ ภาพซ้อน + เพลงที่อัปโหลดไว้ — สร้างใหม่เองไม่ได้
└─ fxseg/             <content-hash>.mov  ชิ้นที่ใส่เอฟเฟกต์แล้ว
```

ลบ `.vcut/` ทั้งโฟลเดอร์ได้เสมอ — สร้างใหม่ได้จากฟุตเทจ + config
**ยกเว้น `assets/`** ซึ่งเก็บไฟล์ที่คนอัปโหลดเข้ามาเอง ไม่มีทางสร้างใหม่จากอะไรได้
(กล่องรีเซ็ตติดกรอบแดงไว้ให้ด้วยเหตุผลนี้)

`captions.json` กับ `fx.json` commit เข้า git ได้เหมือน `edl.json` — ทั้งคู่เป็น
*การตัดสินใจ* ไม่ใช่ผลงานที่ทำใหม่ได้

`vcut gc` ลบเฉพาะ segment ที่ EDL ปัจจุบันไม่ได้ใช้ **และชิ้นของขั้น 5 ที่ `fx.json`
ปัจจุบันไม่ได้ใช้** · `vcut gc --all` ลบทั้งหมด
ลบทีละอย่างพร้อมเห็นราคาของการทำใหม่ก่อน ใช้ `vcut reset --files ...` หรือกล่องรีเซ็ต

⚠️ `vcut gc` คำนวณรายการที่ต้องเก็บจาก `--work` ที่ชี้ไปเท่านั้น — **อย่าเอาโฟลเดอร์
ทดสอบไป symlink ทับ `segments/` ของโปรเจกต์จริง** มันจะลบตาม EDL ของโฟลเดอร์ทดสอบ

`.vcut-history/` (คนละที่กับ `.vcut/`) เก็บสำเนาไฟล์โปรเจกต์ก่อนกดรีเซ็ตทุกครั้ง
ลบทิ้งได้เหมือนกัน แค่จะหมดตัวเลือก "กู้คืน" ไปด้วย

**ถ้ามี transcript จากงานรอบก่อนอยู่แล้ว** ชี้ `[listen] import_dir` ไปที่โฟลเดอร์นั้น
จะข้ามขั้นถอดเสียงทั้งหมด (ประหยัดสิบกว่านาทีต่อชั่วโมงฟุตเทจ)

---

## ข้อจำกัดที่ควรรู้

- `[render] workers` ตั้งไว้ 2 เพราะ videotoolbox บน M3 เป็นคอขวดอยู่แล้ว เพิ่มแล้วไม่เร็วขึ้น
- `listen` ถอดเสียงทีละคลิปเรียงกันไป ไม่ขนาน — whisper large กิน RAM หนักเกินกว่า
  จะรันพร้อมกันบนเครื่อง 8 GB
- `assemble` แบบ stream copy ต้องการให้ทุก segment เข้ารหัสด้วยพารามิเตอร์เดียวกัน
  เปลี่ยน `[encode]` แล้วต้อง render ใหม่ทั้งหมด
- ความยาวไฟล์ผลลัพธ์อาจต่างจาก EDL ระดับเสี้ยววินาที จาก keyframe ที่ไม่ตรงพอดี
- `select` ตัดด้วยกฎ (ความหลากหลายของคำ + ความนิ่งของภาพ) **ไม่ใช่ความเข้าใจเนื้อเรื่อง**
  การแบ่งบทตามการเดินทางต้องรอ Phase 4
- ขั้น 4 กับขั้น 5 ต้องเข้ารหัสภาพใหม่ทั้งเรื่อง — เขียนตัวหนังสือลงภาพแปลว่าต้องแตะ
  พิกเซล จะ `-c copy` ต่อไปไม่ได้อีก (แก้ข้อความแล้วต้องรอเท่าเดิมทุกครั้ง ไม่มี cache
  ระดับไฟล์รวมให้ — ต่างจาก `assemble` ของขั้น 3 ที่ต่อใหม่ในไม่กี่วินาที)
- ขั้น 5 ต้องมี `ffmpeg-full` เหมือนขั้น 4 · ขั้น 1–3 ยังใช้ ffmpeg ตัวไหนก็ได้
- ซูมของขั้น 5 ทำจากภาพ 1080p ที่ย่อมาแล้ว ไม่ใช่จาก 4K ต้นฉบับ — ซูมเกิน ~1.5 เท่า
  เริ่มเห็นความคมลด (แลกกับการไม่ต้องก๊อปตรรกะปรับมาตรฐานของขั้น 3 มาไว้อีกชุด)
- แอนิเมชัน `rise`/`slide` ใช้ไม่ได้กับข้อความที่ไม่ได้ตรึงพิกัด — ตกกลับเป็น `fade`
- ยังไม่มีทรานซิชันระหว่างช็อต (จาง/ปัด) และยังไม่มีตัวให้ AI เลือกเอฟเฟกต์ให้เอง
